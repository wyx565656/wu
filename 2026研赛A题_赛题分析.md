# 2026年（华为）A题 通用神经网络处理器下的多核调度问题 —— 赛题分析

> 分析基准：题目正文 + 附件包（`README.md`、`code/` 10个评估与核内调度脚本、`data/case_001.json ~ case_100.json` 共100个测试用例 + `config.txt`）
> 输出要求：不套固定模型、优先问题适配性、结论仅作为后续建模方案设计依据

---

## 一、赛题背景分析

**【问题背景】**
单核 AI 核心受制造成本、功耗与散热限制已难以独立满足算力需求，多核 NPU（N个同构 AI 核心 + 共享核外主存 DDR）成为主流架构。待调度对象是一张有向无环计算图 `G=(V,E)`：张量节点 `T`（含位置 `pos∈{DDR,L1,UB}` 与字节数 `size`）、操作节点 `O`（含类型 `COPY_IN/COPY_OUT/ADD/MUL`、执行管道 `PIPE_M/PIPE_V/PIPE_MTE2/PIPE_MTE3`、周期数 `cycles`），边只允许 Tensor↔Op。本赛题要解决的是：把计算图**切分为若干子图 → 分配到各核心 → 确定每核内的子图执行顺序**，在数据依赖、缓存容量、流水线、共享带宽约束下压缩总执行时间。

**【现实意义】**
这是国产 NPU（华为昇腾类）实际部署中的核心工程问题：切图与调度的好坏直接决定算力利用率、端到端时延、DDR 带宽压力与系统能效。题目面向两种真实硬件形态（有无核间同步机制）和两级存储层次（私有 L1/UB + 共享只读 L2），结果可直接指导编译器/运行时的图划分与调度策略。

**【数学建模目标】**
用数学方法刻画三类耦合决策（切图、子图分核、核内子图排序）与三项评价指标（Makespan 为主、总额外数据搬运量、Cache 命中率）之间的定量关系，并设计能在 2~5 核下稳定、高效产出高质量方案的求解算法。本质上是一个**带容量约束与带宽争用的 DAG 图划分 + 调度联合优化问题**。

---

## 二、问题拆解

### 【问题1分析】场景A（无核间同步）下的多核切图与调度
- **问题目标**：建立场景A（1个子图 = 1个 Task，跨子图数据全部经 DDR 中转，核内缓存被清空，`Cinner=Ccross`）的切图与调度模型并求解；在整题中承担"基础模型 + 基线性能"的作用。
- **已知信息**：100 个计算图 JSON；`config.txt` 固定配置——L1=524288 B、UB=131072 B、DDR 总带宽 60 bytes/cycle；同核 Task 切换等待 ≥100 cycles、跨核前驱 Task 完成后等待 ≥1000 cycles；核内调度算法给定（`schedule_step1/2/3`：确定性拓扑序 → 容量不足时自动换出/换入 → 4 条 Pipe 乱序流水排布）。
- **求解目标**：输出 `node_to_subgraph`（全部非 COPY 操作 → 非负整数 sgid，必须恰好覆盖）与 `core_schedules`（N 个列表，每个 sgid 恰好出现一次、同核顺序满足子图依赖）两个字段；报告 1~5 核平均加速比折线图、逐用例 Makespan 与额外搬运量。
- **数学本质**：约束组合优化 / 图划分 + 列表调度（属 NP-hard 家族）；目标函数由外部模拟器给出，属于**黑箱、不可微、非解析的联合优化**。
- **主要难点**：
  1. 三个决策相互耦合：切细→并行机会多但跨子图 DDR 搬运和重复读取增加；切粗→通信省但并行度低、L1/UB 容量压力与核内换入换出剧增。存在"合并后负时间收益"的非单调现象。
  2. 目标不可解析：Makespan 由评估器按共享带宽并发争用重新结算（公平共享 + 剩余工作量重算预计完成时刻），无法写成闭式目标，只能用"外层搜索 + 内层评估器"闭环。
  3. 规模压力：单图 JSON 最大 14.4 MB（`case_014`），`case_025` 8.9 MB、`case_062` 8.7 MB，节点/边数量级大，必须限定算法复杂度。
  4. 加速比口径：单核基准由同一核内调度算法在整图上计算，定义自洽性必须保证。
  5. 还有隐藏约束：Cube（PIPE_M）与 Vector（PIPE_V）在同一核内可并行，切图时须兼顾两个计算单元的负载平衡，否则"单核内自动并行"红利丢失。

### 【问题2分析】场景B（有核间同步）下的多核切图与调度
- **问题目标**：场景B 下同一核心的全部子图合并为一个 Task，同核依赖可复用 L1/UB 中的驻留数据（不插边界 COPY），跨核依赖通过 `COPY_OUT/COPY_IN` + **500 cycles 固定同步延迟**完成；在本核复用收益与缓存压力之间取得更优 Makespan 与更少额外搬运。
- **已知信息**：问题1 的全部数据与配置；差异在于 Task 构造规则、`Cinner<Ccross`、同步延迟 500 cycles、**必须严格满足 L1/UB 容量约束**（不满足则 Task 失败）。
- **求解目标**：场景B 下 1~5 核平均加速比折线图；逐用例 Makespan 与额外搬运量；可复现 Python 脚本。
- **数学本质**：在问题1 模型上叠加"任务级容量约束 + 核内数据复用收益"的划分-调度问题，目标函数新增"驻留代价"（驻留张量持续占缓存，压缩其他子图可用空间）。
- **主要难点**：
  1. **容量硬约束**：子图过大时核内调度可能因单个张量超容量或找不到合法换出对象而直接失败，因此切图必须引入"子图规模/峰值驻留 ≤ L1、UB"的可行域判定。
  2. 驻留的"双刃剑"：跨多子图驻留减少搬运却压低单子图调度效率，收益非单调。
  3. 问题1 的分核结果可能不再最优（Task 合并改变了通信结构），需要重新设计分核准则。

### 【问题3分析】共享 L2 只读 Cache 下的切图与调度
- **问题目标**：在场景B 基础上引入所有核心共享的只读 L2（容量 1 MB、带宽 250 bytes/cycle、**带宽独立于 DDR 不互相占用**、FIFO 淘汰、单 tensor 大于 Cache 容量则不缓存），研究 L2 容量/带宽对切图与调度的影响并建立模型、设计算法。
- **已知信息**：问题2 全部内容 + L2 参数；Cache 命中发生在 `COPY_IN` 发射时按逻辑 tensor id 查询。
- **求解目标**：1~5 核下"无 L2"与"只读 Cache"两种配置的对比曲线；相同核数下只读 Cache 相对无 L2 基线的加速比；逐用例 Makespan、额外搬运量、**Cache 命中率**。
- **数学本质**：在原有优化上引入**共享缓存复用最大化**子目标——即"把共享同一输入的操作聚合到同核/同子图"的组合优化，同时复用命中率反作用于 DDR 流量。
- **主要难点**：
  1. 共享输入复用与并行度、负载均衡三者的冲突：聚合能提高命中率但牺牲并行。
  2. 命中率取决于访问序列与 FIFO 淘汰，涉及缓存行为建模，需借助 `tenant id` 访问顺序做近似分析。
  3. 需要两套配置的对照实验设计与诚实的"相对基线"报告。

---

## 三、小问逻辑关系分析

**【问题链分析】**
- 问题1 → 问题2：**通信模型升级**。问题1 把所有跨子图依赖强制经 DDR，问题2 允许同核驻留复用并把核内子图合并为 Task。这使问题1 的解在问题2 场景下不再最优（分核时"同核"的收益被放大），但问题1 的切图框架、合法性检查与核内调度复用可直接继承。
- 问题2 → 问题3：**存储层次升级**。加入共享只读 L2 后，"重复读取同一输入"的代价从 DDR 带宽降低为 L2 带宽，切图的偏好从"减少通信边"转向"最大化共享输入聚合"，是对问题2 最优结构的一次系统性偏移，属于"参数环境变化导致结构变化"的递进。
- 三问共同依赖同一"评估器即目标函数"的黑箱闭环：切图方案 → Task 构造与跨 Task 搬运重建 → 核内确定性顺序与换入换出 → 多核共享带宽并行模拟 → Makespan。**该链路是所有算法设计必须尊重的硬接口。**

**【整体解题思路】**
1. **对齐接口**：先读 `README.md`、`code/contest_io.py`、`evaluation_validation.py`，把输出格式、合法性检查、评估器行为完全搞清楚（这是拿分前提）。
2. **建立可解析的代价代理模型**：用"切边通信量 + DDR 搬运量 + 关键路径长度 + 各核负载不均衡度 + L1/UB 峰值压力"构造近似目标，替代昂贵的模拟器调用。
3. **分层求解**：① 切图（多级/层次聚类，权重=通信字节数，容量约束=缓存峰值）→ ② 分核（列表调度/LPT/关键路径优先，追求负载均衡与跨核通信最小）→ ③ 核内序（拓扑序 + 关键路径优先 + 复用优先）。
4. **闭环精化**：以评估器为真目标，做迭代局部搜索/模拟退火（邻域：子图合并、拆分、跨核迁移、核内交换），配合早停与缓存复用（同一方案只评估一次）。
5. **泛化验证**：在 100 个用例 × 2~5 核上批量跑通，输出加速比折线；问题3 做无 L2 / 有 L2 的对照。

---

## 四、关键变量分析

| 变量类型 | 内容 |
|---|---|
| **输入变量** | 计算图 `G=(T,O,E)`：张量 `{id,pos,size}`、操作 `{id,op,pipe,cycles}`、边 `{source,target}`；核心数 `N=2~5`；硬件配置 `config.txt`（L1=524288 B、UB=131072 B、DDR 带宽=60 B/cycle、同核切换等待=100 cycles、跨核等待=1000 cycles、同步延迟=500 cycles、L2=1 MB/250 B/cycle） |
| **输出变量** | 切图-调度方案 `node_to_subgraph`、`core_schedules`；派生指标 Makespan、`added_copy_bytes`、Cache 命中率；诊断量：各核负载、关键路径、L1/UB 峰值、缓存换入换出量、COPY 独占带宽参考工作量 |
| **决策变量** | ① 每个非 COPY 操作的 sgid 归属；② 每个子图分配到的核心；③ 同核内子图执行顺序；④（隐含）子图粒度与数量（不加限制，是真正的"自由度之源"） |
| **影响因素** | 子图规模与数量、跨子图边数与字节量、共享输入的被复用次数（fan-out）、PIPE_M/PIPE_V 负载平衡、计算与搬运的可重叠度、跨核依赖链长度（关键路径）、同核多子图的驻留峰值、L2 命中率 |
| **约束条件** | 节点覆盖且无重复；sgid 非负整数；每子图恰好调度一次；子图依赖图必须无环（存在合法拓扑序）；同核顺序不得违反依赖；L1/UB 容量约束（问题2/3 严格）；DDR 总带宽 60 B/cycle 被所有 COPY_IN/COPY_OUT/换入换出共享；Pipe 串行与依赖约束；问题1 的 1000/100 cycles 等待与问题2/3 的 500 cycles 同步延迟；L2 容量 1 MB 与 250 B/cycle 独立带宽 |

**关键变量的作用与影响**
- `node_to_subgraph` 是**放大器**：直接决定跨子图通信量、并行空间与缓存压力，是三个决策中杠杆最大的一环。
- `core_schedules` 的核内顺序影响**依赖等待时间与张量驻留时长**；在 L1/UB 紧张时会触发换出/换入，直接推高 `added_copy_bytes` 并拖长 Makespan。
- `size` 是带宽与容量的共同度量：它既是通信量，也是容量占用，因此**同一变量同时出现在两类约束中**，这是本赛题耦合的本质来源。
- `PIPE_M/PIPE_V` 的并存意味着"负载均衡"要在两个计算单元上分别考虑，单纯的节点数均衡会低估核内空洞。
- `N`（核数）是外部参数而非决策量，但题目要求算法对 2~5 核稳健，因此算法必须显式参数化对核数的适应策略（分核数 vs 负载粒度）。

---

## 五、初步建模方向判断

**【可能建模方向】**
1. **两阶段"划分 + 列表调度"启发式**（首选框架）：子图划分用带容量约束的多级划分/层次凝聚聚类（凝聚准则 = 单位通信字节的收益/缓存代价比），分核用改进 HEFT/LPT（关键路径优先 + 负载均衡修正），核内序用拓扑序 + 关键路径优先。
2. **黑箱闭环的元启发式**：模拟退火/迭代局部搜索/遗传算法，邻域定义为"子图合并、子图拆分、子图跨核迁移、核内序交换"，目标函数为评估器返回的 Makespan（+ 额外搬运量惩罚）。
3. **近似代价模型（代理目标）**：构造 `Makespan ≈ α·(关键路径核内计算量) + β·(DDR 搬运量/60) + γ·(跨核同步延迟) + δ·负载不均衡` 的解析代理，用于快速筛选候选方案，再用评估器精评 Top-K。
4. **问题3 专用的缓存感知划分**：把"共享输入复用"建模为带奖励的 min-cut（同一输入的被使用点放同侧获得复用奖励），并做 L2 容量/带宽敏感性分析。
5. **小规模精确求解**：对最小用例建 MILP/CP 模型，求下界并验证启发式的次优程度（论文中用于论证，不作主算法）。

**【判断依据】**
- 评估器**只接收两个字段**（`node_to_subgraph`、`core_schedules`），且目标值由其黑箱模拟给出 → 任何方法都必须是"外层搜索 + 内层评估"的结构，不可能是纯解析最优化。
- 图规模达 10⁴ 量级、100 个用例、且要求"合理时间内"出解 → 精确法不可行，必须启发式/元启发式，且强调确定性可复现（评估器核内序是确定性的）。
- 题目明确给出"算法优化方向"（通信成本、并行度、重复读取、Cube/Vector 并行、核内启发式效率、驻留占缓存）——这实际上提示了**代价模型应该包含的六项**，应当把它们分别显式写进代理目标，而不是笼统地做图划分。
- L1/UB 容量是硬约束且会导致 Task 失败 → 需要**可行性前置判定**（子图峰值驻留估计），否则搜索大量落在不可行域。

---

## 六、赛题难点总结

**【核心问题】**
在"多核并行度 ↔ 跨子图通信/DDR 搬运 ↔ 核内缓存容量"三者之间做出**联合最优**的权衡。任何单环节的孤立优化都会在另两个环节上反噬，真正的矛盾是：切图粒度同时决定了并行空间、通信边界与缓存压力，而这三者对粒度单调性方向相反。

**【关键难点】**
- **数据**：单图 14 MB、节点边规模大；评估器内部（带宽公平共享、FIFO Cache、换入换出）行为需通过阅读源码/文档反推，存在"黑箱接口"理解成本。
- **模型**：目标函数不可解析、非光滑、带事件驱动模拟；容量约束是硬约束且会致方案失败；三个决策耦合导致解空间巨大。
- **算法**：必须同时满足"高效率（合理时间出解，100 用例批量）"与"高质量（高加速比、低额外搬运）"，并对 2~5 核稳健。
- **应用**：需要给出可复现脚本与逐用例结果表，且加速比定义必须与单核基准自洽。

**【后续建模重点】**
1. 先做**基线方案**（单核内调度在整图上的 Makespan 作基准 → 拓扑序 + 贪心分核）取得可信起点。
2. 精读 `docs/核内调度算法.md`、`docs/多核并行模拟执行算法.md`，把"自动换入换出"与"共享带宽结算"规则转写为可计算的代价项。
3. 建立**六项代价代理模型**（通信、并行、重复读取、Pipe 负载均衡、核内换入换出、缓存驻留）并做权重敏感性分析。
4. 设计**可行性判定**（子图峰值驻留 ≤ L1/UB），把容量约束前置到切图阶段。
5. 以评估器为真目标做**局部搜索精化**，并按题目要求的图与表（加速比折线、逐用例 Makespan/额外搬运量、L2 命中率）组织结果。

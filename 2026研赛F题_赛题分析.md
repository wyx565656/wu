# 2026年F题 算力约束下提升大语言模型能力的资源配置建模 —— 赛题分析

> 分析基准：题目正文 + 附件 A（A1–A18：质量信号 A1–A3、配方实验 A4–A15、跨域映射 A16、验算 A18）+ 附件 B（B1–B12：B1 主拟合、B2/B3 验证、B4/B5 跨族、B6–B8 半合成、B9/B10 百亿以上外推）+ 附件 C（C1–C10：C7 上下文长度可行性、C5/C6 Loss–Benchmark 桥接、C4 算力/数据/开源、C8 逐任务聚合）+ 附录 B 成本函数与领域名录
> 输出要求：不套固定模型、优先问题适配性、结论仅作为后续建模方案设计依据

---

## 一、赛题背景分析

**【问题背景】**
以大模型（LLM）训练类比"培养学生"：参数量 N=脑容量，训练数据量 D=阅读量，计算量 C=教育经费，数据质量 Q=教材质量，领域配比 p=各科课时分配。标度律（Scaling Law）刻画损失随规模的经验关系 `L(N,D)=E+A·N^-α+B·D^-β`（Loss 指验证集交叉熵损失）。随着干净语料稀缺、数据污染与"数据墙"问题突出，2026 年《Nature》研究指出仅 80 亿参数的 OpenScholar-8B 在若干任务上优于 GPT-4o，说明 Q 与 p 不可忽视。扩大规模、提升质量、延长上下文都需算力，但算力有限；上下文越长，注意力与 KV 缓存开销越高，挤压参数/数据/质量预算；调领域配比不增总算力却改变分配效果。四者存在取舍。题目要求用真实公开数据与已标注的半合成补充数据，建立 N、D、Q、p 与 Loss 的关系模型，并研究算力约束下的资源配置。

**【现实意义】**
这是把"大模型训练资源配置"这一产业级决策问题，转化为可建模、可优化、可预测的数学问题：① 先回答"数据本身好不好、怎么配"（问题1）；② 再把数据因素纳入标度律，量化各因素边际效用与替代关系（问题2）；③ 在算力预算下求最优配置并识别"预算跨越量级时策略是否质变"（问题3）；④ 用历史评测分离"规模扩张 vs 技术进步"各自的贡献，预测能力前沿（问题4）。对研究生建模竞赛，它考查的是**统计建模（标度律回归）、约束优化（资源分配）、时间序列/因果分解（演进分析）与不确定性量化**的综合能力，而非底层系统开发。

**【数学建模目标】**
用模型刻画：① 22 维质量指标→综合质量分 Q 的合成与冲突消解，及 17 域配比 p 与 Loss 的定量关系（问题1）；② 含 N、D、Q、p 的广义标度律及因素弹性/替代条件（问题2）；③ 预算 C 下 N、D、p、Q 的联合优化与结构性转移识别（问题3）；④ 规模扩张与非规模技术进步的贡献分离与能力前沿预测（问题4）。本质是**以 Scaling Law 为核心、以算力预算为约束、贯穿"数据—规模—能力"的统计建模与资源优化问题**。

---

## 二、问题拆解

### 【问题1分析】数据质量评价、质量冲突消解与领域配比建模
- **问题目标**：对质量指标预处理→综合评价→样本/语料/领域级质量分 Q（方向统一"越高越好"）；定义并消解"质量冲突"；用 A4–A15 建立 17 域配比 p 与交叉熵损失的定量关系，分析各领域/组合影响。
- **已知信息**：质量信号 A1（抽样集）与 A2、A3（扩展集）全部记录用于质量评价，须给出域级 Q 并与抽样集对照；质量指标 22 维、方向不一（如教育价值越高越好、广告含量越低越好，须做补转换 1-x）；"冲突"指不同指标对同一文本评价显著不一致（如教育价值高且广告含量高），须定义并给消解规则；配方实验 A4–A15 域分类体系与质量信号不同，若引 Q 须自建跨体系关联（A16 参考映射）；配比满足单纯形约束 `p_i≥0, Σ p_i=1`；模型在检验集 A6–A11 验证，用外推表 A12–A15 讨论稳健性；可选用 A18 验算。
- **求解目标**：① 样本/语料/领域级 Q 与聚合方式；② 冲突定义、成因、消解后的综合评价；③ 17 域 p 与 Loss 关系及领域/组合影响；④ 检验集验证 + 外推稳健性。
- **数学本质**：**多指标综合评价（加权/主成分/TOPSIS 等）+ 单纯形约束下的回归/响应面建模**，偏统计与多属性决策。
- **主要难点**：
  1. **指标方向统一与多维列表型压缩**：22 维中部分为负向须补转换，部分"多维列表型"须先压缩为标量再参与评价——压缩与归一化方式会显著影响 Q，需论证。
  2. **"质量冲突"的定义与消解是开放题**：题目只说"显著不一致"，未给阈值/度量。须自行定义（如指标标准化后符号相反且幅度超阈值=冲突），并给可辩护的消解规则（如惩罚项、加权折中、分情形处理），还要在扩展集检验结论是否稳健——这是评分的区分点。
  3. **跨体系关联的主观性**：质量信号（A1–A3）与配方实验（A4–A15）域体系不同，把 Q 引入配比建模须建映射（A16 仅参考），映射假设直接决定问题2 的输入质量，须显式说明假设与局限。
  4. **单纯形约束下的回归**：p 在 17 维概率单纯形上，自由度 16，存在共线性与边界效应；需用合适参数化（如 softmax/对数比/中心化）避免冗余，并在检验/外推集分别评估，外推到 A12–A15 配比可能超出训练分布，需报告外推不确定度。

### 【问题2分析】跨维度数据融合与广义标度律
- **问题目标**：在经典 `L(N,D)=E+AN^-α+BD^-β` 基础上，综合附件 A、B 建立同时含 N、D、Q、p 的广义标度律（Q=1 时退化为经典形式为自然约束，可自行设计）；分析各因素边际效用与弹性，讨论领域间替代/互补，推导"提升质量与扩大规模可相互替代"的条件，完成参数估计、检验与验证。
- **已知信息**：附件 A 与问题1 同源（经问题1 的 Q、p 引入，不直接读原始）；本问直接用附件 B（B1–B12，B11/B12 辅助）。B1 为主拟合；B2/B3 之一做模型族外/插值验证；B4/B5 做跨族/文献验证；质量相关用 B6（或 B7/B8 半合成），结合 B9/B10 讨论百亿参数以上外推。B6–B8 为基于真实数据校准的半合成补充集，不得表述为直接观测，须标可信度边界。广义形式自定，鼓励创新。
- **求解目标**：① 广义标度律形式（含 N,D,Q,p）；② 参数估计（拟合 B1）；③ 边际效用与弹性；④ 领域替代/互补关系；⑤ 质量提升⇔参数增加的可计算条件；⑥ 多源验证（B2–B5、B9/B10）与半合成可信度标注。
- **数学本质**：**含质量与配比的高维标度律回归（非线性最小二乘/贝叶斯拟合）+ 弹性/边际分析**，属经验定律的泛化与统计推断。
- **主要难点**：
  1. **广义标度律的函数形式设计**：如何把 Q、p 纳入且不破坏经典形式（Q=1 退化）是关键创新点。常见设计：把有效数据量 D 替换为 `D·h(Q)` 或把 N、D 乘质量增益因子；p 可进入领域加权损失或作为截距/斜率修正。形式选择需有理论/经验依据，并用 B 系列多源数据检验。
  2. **多源数据统一到同一模型**：附件 A、B 实验独立但都以交叉熵为观测，须提"可检验假设"把不同来源规律统一（如同一 α、β 跨族），再用 B4/B5 做跨族验证——统一假设的合理性决定外推可信度。
  3. **半合成数据的可信度边界**：B6–B8 是"基于真实校准的半合成"，只能作补充分析、不得当直接观测；须在文中明确其局限与可信区间，否则会高估外推精度。
  4. **弹性与替代条件的可计算化**："质量提升 0.1 等价于参数增加多少"须从广义律解出隐式关系（如固定 Loss 下 dN/dQ 或 d(log N)/dQ），需对形式求导并给出可计算闭式或数值条件，是本题的"定量分析"亮点。
  5. **百亿参数以上外推风险**：B9/B10 用于 >10B 外推，但标度律在大尺度可能失效（涌现/相变），须报告外推不确定度与不保真区间。

### 【问题3分析】算力约束下的多维资源联合优化与结构性转移
- **问题目标**：在总算力预算 C 下，分配 N、D、p、Q，分析预算跨越量级时最优配置是否"质变"（结构性转移），并给数学定义与识别方法；考察低/中/高三档 C=10^19/10^22/10^24 FLOPs；Lctx 外生给定（依 C7），不作为内点寻优变量，只考察其影响；讨论成本函数选择对最优解的影响。
- **已知信息**：成本三部分——基础训练 `C_train=6ND`（Chinchilla 近似）；质量提升 `C_Q=D·[g(Q)-g(Q0)]₊`，g(Q) 三选（指数 γ=1e7,λ=6；幂 γ=5e9,λ=4；对数渐近 γ=2e9,λ=10）；长文本注意力 `C_attn=η·N·D·L_ctx`，η=2e-4。须解析给出 `L_ctx_crit=6/η=3×10^4`（使注意力开销≈训练开销的临界上下文长度）并在可行取值上做敏感性分析。p 可作联立决策或取前两问结果（须说明）。默认训练/质量/注意力用同一 D。
- **求解目标**：① 预算 C 下 N,D,p,Q 的最优配置（min Loss s.t. C_train+C_Q+C_attn≤C）；② 成本函数（g(Q) 三型）选择对最优解的影响；③ 结构性转移的数学定义与识别；④ Lctx_crit 解析与 Lctx 敏感性。
- **数学本质**：**带非线性的约束优化（标度律为近似目标、算力为约束）+ 参数/预算扫描下的相变识别**，属资源分配优化。
- **主要难点**：
  1. **目标与约束都非线性且耦合**：Loss 随 N、D 幂函数下降，但 C_train=6ND、C_Q 依赖 D 与 g(Q)、C_attn 依赖 N·D·Lctx，变量高度耦合；解析 KKT 难求闭式，需数值优化 + 多起点/网格，且要防局部最优。
  2. **质量成本 g(Q) 三型差异巨大**：指数/幂/对数渐近三型在 Q 接近 1 时成本量级差异悬殊（γ 从 1e7 到 5e9），会彻底改变"堆参数 vs 提质量"的取舍，须对三型分别求解并比较——这是明确的"成本函数敏感性"考点。
  3. **结构性转移的严格定义**：不能只说"变了"，须数学定义（如最优解在预算轴上的分段常数/分段解析、或效用无差异曲线斜率变号、或主导约束切换），并给识别方法（如扫描 C 看最优组合突变点、或看影子价格/边际替代率符号翻转）。
  4. **Lctx 临界值与敏感性**：须解析推 `L_ctx_crit=6/η`（由 C_attn=C_train → η·N·D·L=C_train=6ND → L=6/η），并在 C7 给定可行取值上做敏感性——长上下文会"吃掉"多少本可用于 N/D 的预算，是结构性转移的重要触发因素。
  5. **p 的处理口径**：p 联立决策（17 维单纯形内优化）还是取问题1/2 结果固定，须说明理由；联立会增加维度与复杂度，固定则依赖前两问质量。

### 【问题4分析】技术演进分析与前沿预测
- **问题目标**：结合附件 C，建立动力学或因果推断模型，分离并量化"规模扩张"与"非规模技术进步"各自贡献占比；在算力增长放缓情景下，预测未来 12/24 个月开源 LLM 能力前沿，给不确定性分析；综合能力度量须说明；开源筛选口径（权重/许可证/复现）、模型类型（pretrained vs chat/finetuned）、时间轴口径（提交/发布/版本日期）须说明；用 C5/C6（或文献）建立 Loss–Benchmark 映射并讨论误差影响。
- **已知信息**：前三问用交叉熵 Loss，本问用多维 Benchmark 得分（0–100）；须用 C1/C2 与 C3、C4 的算力/数据/开源权重字段、C8 至少一项逐任务聚合（不得仅用汇总表）；桥接数据按可比性等级区分；C7 上下文长度可行性可辅助。
- **求解目标**：① 规模扩张 vs 非规模进步的贡献分离与占比；② 未来 12/24 月能力前沿预测 + 不确定性；③ Loss–Benchmark 映射与误差讨论；④ 开源筛选/模型类型/时间轴口径定义；⑤ C8 逐任务聚合分析。
- **数学本质**：**时间序列/面板数据的因果分解（规模效应 vs 技术进步）+ 外推预测与不确定性量化**，属演进动力学与预测。
- **主要难点**：
  1. **规模 vs 非规模贡献的可识别性**：需控制 N、D、C 等规模变量后，把残差/趋势归因于"技术进步"，本质是去趋势或反事实（如固定规模看 Loss/Benchmark 随时间下降多少）。识别假设（如技术进步不随规模变化）须显式，否则贡献拆分主观。
  2. **Loss–Benchmark 桥接误差**：前三问用 Loss，本问用 Benchmark，二者相关不等价；映射（C5/C6 或文献）有误差，须讨论其对贡献占比与前沿预测的影响，并按可比性等级分级使用桥接数据。
  3. **开源筛选/类型/时间轴口径**：口径不同结论大异（如含 chat 模型会高估"能力"、按发布日期会高估时效），须定义清晰且可辩护，最好做口径敏感性。
  4. **预测的不确定性**：算力放缓情景下前沿预测须给区间（如 bootstrap/贝叶斯后验/情景法），不能只给点估计；需说明外推不确定度来源（数据稀疏、架构突变、评测饱和）。
  5. **C8 逐任务聚合**：题目要求至少一项逐任务（per-task）聚合，不得仅用汇总——需下钻到具体 benchmark 维度看进步是否均匀，避免被平均掩盖。

---

## 三、小问逻辑关系分析

**【问题链分析】**
- 问题1 → 问题2：问题1 产出**域级 Q 与 17 域配比 p↔Loss 关系**，问题2 用其把 Q、p 纳入广义标度律（题目明确"本问通过问题1 输出的 Q 与 p 引入，不重复读原始"）。问题1 的 Q 质量与跨体系关联假设直接决定问题2 的输入可信度。
- 问题2 → 问题3：问题2 的广义标度律（含 N,D,Q,p 的 Loss 近似）是问题3 优化的**目标函数**；问题2 的弹性/替代条件为问题3 的"堆参数 vs 提质量"取舍提供理论预期。问题3 在算力约束下求最优配置，是问题2 规律的"落地求解"。
- 问题3 → 问题4：问题3 给出"给定预算的最优资源配置"规律，问题4 用历史数据（附件 C）检验"真实演进是否遵循标度律 + 技术进步"并预测前沿，是规律的**实证与外推**。前三问用 Loss，问题4 切换到 Benchmark，需桥接。
- 数据继承：A 系列（质量+配方）→ 问题1/2；B 系列（标度实验）→ 问题2 拟合与验证；C 系列（演进+评测+桥接）→ 问题4；附录 B（g(Q) 三型、17 域/22 指标名录）→ 贯穿问题1/3。

**【整体解题思路】**
1. **问题1**：统一指标方向（负向补转换 1-x）→ 多维列表型压缩为标量 → 选综合评价法（熵权/CRITIC 赋权 + 线性/主成分合成）得 Q，定义冲突（标准化后异号超阈值）并给消解（惩罚/折中）→ 域级 Q 与抽样集对照；配比用单纯形参数化回归（A4–A15）得 p↔Loss，A6–A11 验证、A12–A15 外推稳健性。
2. **问题2**：设计广义标度律（如 `L=E+A N^-α+B (D·h(Q))^-β+Σ p_i·δ_i` 或质量增益因子乘 N/D，Q=1 退化经典）；以 B1 非线性最小二乘/贝叶斯拟合，B2–B5 多源验证，B6–B8 半合成标可信度边界，B9/B10 百亿外推；对形式求导得弹性与"质量⇔参数"替代条件。
3. **问题3**：以广义律为近似目标、算力 `C_train+C_Q+C_attn≤C` 为约束做数值优化（多起点/网格/KKT 近似）；对 g(Q) 三型分别求解比较；扫描 C=10^19/10^22/10^24 识别结构性转移（定义最优组合突变/影子价格翻转）；解析推 `L_ctx_crit=6/η` 并做 Lctx 敏感性。
4. **问题4**：用附件 C 面板数据做去趋势/反事实分解，分离规模 vs 技术进步贡献；建 Loss–Benchmark 桥接（C5/C6，分级）；预测 12/24 月前沿并给不确定性；定义开源/类型/时间轴口径，C8 逐任务聚合。

---

## 四、关键变量分析

| 变量类型 | 内容 |
|---|---|
| **输入变量** | 质量：A1 抽样集 + A2/A3 扩展集（22 维指标，方向不一）、A16 跨域映射、A18 验算；配方：A4–A15（17 域配比 + Loss，A6–A11 检验、A12–A15 外推）；标度：B1 主拟合、B2/B3 验证、B4/B5 跨族、B6–B8 半合成、B9/B10 百亿外推、B11/B12 辅助；演进：C1/C2/C3、C4（算力/数据/开源权重）、C5/C6（Loss–Benchmark 桥接）、C7（上下文长度可行性）、C8（逐任务 benchmark）；参数：η=2e-4、g(Q) 三型系数、17 域与 22 指标名录 |
| **输出变量** | 域级/样本级 Q 与冲突消解规则；p↔Loss 关系与领域影响；广义标度律形式与参数；各因素边际效用/弹性与质量⇔参数替代条件；预算 C 下最优 N,D,p,Q 与结构性转移识别；L_ctx_crit 与敏感性；规模 vs 非规模贡献占比；12/24 月能力前沿预测与不确定性；Loss–Benchmark 映射 |
| **决策变量** | 问题1：22 维指标赋权/压缩方式、Q 合成公式、冲突阈值与消解规则、p 参数化；问题2：广义标度律函数形式、如何纳入 Q/p、拟合方法；问题3：N,D,Q,p（p 可联立或固定）、g(Q) 选型；问题4：演进分解模型、桥接函数、开源/类型/时间轴口径 |
| **影响因素** | 指标方向处理与压缩方式、Q 合成赋权、跨体系映射假设、配比共线性与边界、标度律形式选择与跨族一致性、半合成可信度、质量成本 g(Q) 选型、预算量级、Lctx 取值、规模 vs 技术进步的可识别性假设、桥接误差、开源筛选口径 |
| **约束条件** | 质量指标统一"越高越好"、须用全量质量信号；配比单纯形 `p_i≥0,Σp_i=1`；广义律建议 Q=1 退化经典（须说明依据）；算力 `C_train+C_Q+C_attn≤C`；Lctx 外生依 C7、非寻优变量；默认训练/质量/注意力同 D；半合成不得当直接观测、须标可信度；开源筛选/类型/时间轴口径须说明；C8 不得仅用汇总表；禁止引入赛题外数据（问题1） |

**关键变量的作用与影响**
- **质量分 Q 的合成方式**是问题1→问题2 的传递枢纽：赋权/压缩/冲突消解任一主观选择都会沿链路放大到广义标度律与外推结论，须全程可辩护且做敏感性。
- **广义标度律的函数形式**决定全题结论形态：把 Q、p 以"乘性增益"还是"加性修正"纳入，会根本改变"质量⇔参数"替代条件与问题3 的最优结构，须用 B 系列多源数据严格检验。
- **质量成本 g(Q) 三型**是问题3 的最大不确定性源：γ 跨 1e7~5e9 的量级差使"提质量"在不同成本假设下时而划算时而昂贵，必须三型分别求解并比较，否则结论脆弱。
- **预算量级 C**与 **Lctx** 共同触发结构性转移：当 C 小，预算先满足基础训练（温饱）；C 大时质量/上下文占比上升（质变）；`L_ctx_crit=6/η=3×10^4` 是注意力开始吞掉训练预算的临界点，是转移识别的关键锚。
- **演进分解的可识别性假设**（问题4）决定规模/技术进步贡献占比的可信度：假设不同，拆分结果可大异，须显式并做情景/敏感性。

---

## 五、初步建模方向判断

**【可能建模方向】**
1. **问题1**：指标方向统一（负向 `1-x` + 归一化）→ 多维列表型压缩（均值/范数/主成分）→ 客观赋权（熵权/CRITIC，避免主观）合成 Q；冲突定义为"标准化指标异号且|差|超阈值"，消解用加权折中或分情形惩罚；配比用单纯形对数比/softmax 参数化 + 回归或高斯过程响应面得 p↔Loss，A6–A11 留一/hold-out 验证、A12–A15 外推区间估计。
2. **问题2**：广义标度律建议形式 `L=E+A(N·φ(Q))^-α+B(D·ψ(Q))^-β+π(p)`（φ,ψ 为质量增益因子，Q=1 时退化；π(p) 为领域加权修正），或把有效数据 `D_eff=D·h(Q)`；用 B1 非线性最小二乘/贝叶斯（给后验与不确定度）拟合，B2–B5 跨源验证，B6–B8 标可信区间，B9/B10 外推；对形式解析求导得弹性 `ε_Q, ε_N` 与替代条件 `dN/dQ|L=const`。
3. **问题3**：以广义律为近似目标、算力为约束做**数值优化（多起点网格/序列二次规划/KKT 近似）**；对 g(Q) 三型分别求解比较；扫描三档 C 识别结构性转移（定义：最优组合在 C 轴上分段突变或影子价格符号翻转）；解析 `L_ctx_crit=6/η` 并做 Lctx 敏感性（注意力开销占比随 Lctx 变化曲线）。
4. **问题4**：用附件 C 面板/时间序列做**去趋势分解**（控制 N,D,C 后残差趋势=技术进步）或**反事实/因果推断**（固定规模看能力随时间提升）；建 Loss–Benchmark 桥接（C5/C6 分级，讨论映射误差对贡献占比的影响）；前沿预测用外推 + bootstrap/贝叶斯区间；C8 逐任务聚合看进步均匀性；口径敏感性（开源/类型/时间轴）必做。
5. **通用方法层**：全题是统计+优化，建议"小尺度解析 + 大尺度数值/贝叶斯"结合；每个开放选择（Q 合成、标度形式、g(Q) 选型、分解假设）都配敏感性/消融，以"可辩护 + 不确定性量化"取胜。

**【判断依据】**
- 题目**开放度极高**（预处理/模型/求解/指标均自定），但**给了关键约束与数据清单**（附录 A/B、领域与指标名录、g(Q) 三型、Lctx_crit 必须解析）→ 得分点在"把开放选择论证清楚 + 用指定数据严格验证"，而非套现成模型。
- **数据分源且可信度不同**（A 质量、B 标度实验、C 演进；B6–B8 半合成须标边界）→ 必须分级使用、显式标注可信度，否则外推结论不可信。
- **问题3 的非线性耦合 + 三型成本** → 解析难、须数值优化 + 多型比较；结构性转移须严格定义（不能定性带过），这是与标准资源优化最大的区别。
- **问题4 的桥接误差与口径敏感性** → Loss↔Benchmark 不是同物，分解假设主观，必须讨论误差与做口径敏感性，否则贡献占比无说服力。
- 题目强调"AI 不得代写核心建模、须披露"→ 方法须自证自推，文献（Bahri 神经标度律、Hoffmann Chinchilla、Xia RegMix、Biderman Pythia、Asai Nature 2026 等）可作依据但须引用可核验来源。

---

## 六、赛题难点总结

**【核心问题】**
在"数据质量 Q、领域配比 p、参数规模 N、数据规模 D、上下文 Lctx"五要素此消彼长的算力约束下，建立贯穿"数据评价 → 广义标度律 → 预算最优配置 → 演进与前沿预测"的模型链，核心矛盾是**预测精度、数据质量、上下文长度、算力预算四方向不一致的取舍**，而真正的"质变点"往往出现在预算跨越量级（`L_ctx_crit=6/η`）或质量成本假设切换时。

**【关键难点】**
- **数据**：多源（A/B/C）、多可信度（半合成须标边界）、质量与配方域体系不同需自建映射；22 维指标方向不一且含多维列表型，预处理主观性强。
- **模型**：广义标度律函数形式开放但须退化经典并多源验证；质量成本 g(Q) 三型量级差悬殊；结构性转移须严格数学定义；演进分解的可识别性假设敏感；Loss–Benchmark 桥接有误差。
- **算法**：问题3 非线性和 constraint 耦合需数值优化 + 多起点防局部最优；问题4 需面板分解/外推 + 不确定性量化；每个开放选择都需敏感性/消融，工作量集中在"论证 + 验证"而非"实现"。
- **应用**：须按附录 A/B 数据清单严格使用（A6–A11 验证、A12–A15 外推、C8 逐任务），禁用赛题外数据（问题1），AI 使用须披露且不得代写核心建模，结论须带不确定度。

**【后续建模重点】**
1. **问题1 先定 Q 合成与冲突规则**：客观赋权（熵权/CRITIC）+ 显式冲突阈值与消解，域级 Q 与抽样集对照，跨体系映射假设写清。
2. **问题2 广义律形式须可退化 + 多源验证**：用 B1 拟合、B2–B5 跨源、B6–B8 标边界、B9/B10 外推，求导得弹性与替代条件。
3. **问题3 三型 g(Q) 分别求解 + 结构性转移严格定义**：解析 `L_ctx_crit=6/η`，扫描三档预算识别突变/影子价格翻转，Lctx 敏感性必做。
4. **问题4 分解假设显式 + 桥接误差讨论 + 口径敏感性**：规模 vs 技术进步贡献须可识别假设支撑，Loss–Benchmark 分级使用，C8 逐任务聚合，前沿给区间。
5. **全程不确定性量化**：贝叶斯拟合/ bootstrap/情景法贯穿，每个开放选择配敏感性，以"可辩护 + 不确定度"对冲开放题的主观风险。
6. **严格数据清单与合规**：按附录 A/B 使用数据、标半合成边界、禁赛题外数据、披露 AI 使用，避免违规。

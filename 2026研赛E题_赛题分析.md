# 2026年E题 复杂场景下多模态情感预测的数学建模与算法设计 —— 赛题分析

> 分析基准：题目正文 + 附件1（100条原始视频 + label-100.xlsx）+ 附件2（aligned_50.pkl / unaligned_50.pkl，train/valid/test 各 4850 条，文本 50×768、语音 50/500×74、视觉 50/500×35）+ 附件3（模态局部缺失专项测试集）+ 附件4（可解释性专项测试集）
> 输出要求：不套固定模型、优先问题适配性、结论仅作为后续建模方案设计依据

---

## 一、赛题背景分析

**【问题背景】**
情感分析是具身智能、自然人机交互的关键支撑技术。真实交互中人类情绪由**文本语义、语音韵律、面部表情与肢体动作**多通道协同表达，多模态联合建模是突破单模态局限、提升复杂场景情绪理解准确性与稳定性的核心路径。但题目指出落地面临三重挑战：① 三模态在采样频率、特征维度、时间尺度上差异显著，**特征提取与时序对齐质量直接决定模型性能上限**；② 实际采集常因遮挡、噪声、转写失败导致**模态局部缺失**，常规固定权重融合模型性能急剧下降；③ 多数模型只能输出结果，**决策依据难以追溯与复核**，制约可信应用。题目以 CMU-MOSEI 英文多模态情感数据集为基础，依次开展"特征提取与时序对齐 → 缺失条件鲁棒预测 → 可解释预测"三层建模。

**【现实意义】**
这是把"多模态学习"这一机器学习课题，按数学建模竞赛的口径拆成三个递进、且各自可独立评价的子任务：第一层是**数据工程与特征工程**（把异构原始信号变成标准化时序特征，且可核验、可复现）；第二层是**鲁棒性建模**（局部模态缺失下仍稳定预测情感极性与强度）；第三层是**可解释性建模**（不仅要预测，还要给出"依据什么、哪个模态主导、关键片段在哪"的可复核证据）。对研究生建模竞赛而言，它考查的是"如何从原始数据出发，建立一套自洽、可验证、可解释的方法体系"，而非单纯比拼深度学习调参。

**【数学建模目标】**
用明确的数学模型刻画：① 三模态原始信号→标准化时序特征的处理流程与跨模态对齐规则（问题1）；② 局部模态缺失条件下，情感极性（分类）与情感强度（回归）的鲁棒预测模型及缺失影响规律（问题2）；③ 信息完整条件下，可量化、可复核的情感预测及证据定位模型（问题3）。本质是**以 CMU-MOSEI 为数据底座的多模态表示学习、鲁棒融合与可解释推断问题**，其中"对齐"与"缺失处理"是两条贯穿全题的数学主线。

---

## 二、问题拆解

### 【问题1分析】多模态情感特征提取与时序对齐的数学建模
- **问题目标**：基于附件1的 100 条原始视频，建立文本、语音、视觉三模态情感特征提取与时序对齐的数学模型；模型须明确原始样本处理流程、各模态特征定义与提取方法、跨模态时序对齐规则与实现逻辑；可用开源工具/预训练模型完成特征提取基础处理。
- **已知信息**：附件1（37 个子文件夹，每文件夹含 1+ 个 clip_id.mp4；时长 2.648~34.567 s；label-100.xlsx 给出 video_id、clip_id、text 转写、label∈[-3,0)/0/(0,3]、annotation∈{Negative,Neutral,Positive}）；题目允许使用开源特征提取工具与预训练模型，但须标注名称/版本/核心参数。
- **求解目标（交付三项硬性要求）**：① **样本覆盖完整性**——样本编号、模态文件、输出特征一一对应；② **时序组织可核验性**——序列位置、有效长度、填充规则及其与原始素材的对应记录；③ **方法合理性与可复现性**——特征提取工具、关键参数、处理日志、复现说明。还需在论文中以表格汇总 100 条样本特征提取结果（样本编号、模态类型、原始有效时长、特征维度、对齐粒度），并展示至少 1 个典型样本的"文本片段—语音时段—视频帧段—三类特征"对应关系。
- **数学本质**：**多模态特征工程 + 跨模态时间对齐（重采样/动态时间规整到统一时间网格）**，产出标准化时序特征矩阵；偏数据工程与信号处理，评价靠"规范符合度 + 可复现性 + 典型样本对齐展示"。
- **主要难点**：
  1. **三模态原生时间尺度完全不同**：文本是离散词序列（约 50 个 token 位置），语音/视觉是连续高帧率信号（原始可达数百帧）。"对齐粒度"如何定义、"统一时间网格"取多少位置、未对齐与对齐两套版本如何切换，是问题1 必须先行定调的核心。
  2. **对齐规则须可核验**：题目强调"时序组织的可核验性"——序列位置、有效长度、填充规则都要能回查原始素材。这意味着对齐不能是黑箱，必须给出确定的重采样/截断/填充算法，并保留映射记录（否则后续问题2/3 的证据定位无从落地）。
  3. **特征定义须自洽且可复现**：文本可用预计算 BERT（50×768）或 text_bert（3×50 经 BERT 编码）；语音/视觉维度（74/35）由原始提取流程确定，题目明确"不对其每一维物理含义作额外假定"。因此重点不在"发明特征"，而在"选工具 + 定口径 + 留日志"，任何版本/参数不一致都会破坏问题2/3 的输入一致性（题目强制要求训练/验证/测试保持同一特征版本与接口）。
  4. **覆盖完整性是硬约束**：100 条样本须条条对应、无遗漏、无错位；label-100.xlsx 的 video_id/clip_id 与子文件夹/文件名必须严格对齐，错配会直接污染全部下游。

### 【问题2分析】模态信息缺失条件下的情感预测建模与验证
- **问题目标**：针对局部时段模态信息缺失（文本/语音/视觉中部分连续时间段不可用，非整模态缺失），构建鲁棒多模态情感预测模型，在局部缺失下稳定输出情感极性与强度，并分析缺失模态类型、缺失位置、缺失时长对性能的影响规律；用模型对附件3 测试样本推理并提交结果文件。
- **已知信息**：问题1 生成的（或附件2 提供的）三模态时序特征；附件3 为"随机模态缺失"专项测试集——缺失表现为"一个或多个模态中存在特征值全部为零的随机连续序列区间"；问题2/3 模型均在附件2 训练集学习参数、验证集选结构与阈值；附件3/4 为无标签专项测试集，仅用于最终推理。
- **求解目标**：① 鲁棒性模型（结构/目标/训练/超参）；② 缺失类型、缺失率对性能的影响规律与消融实验；③ 附件3 全量预测结果；④ 验证集基础性能评价、可视化与错误归因。评价指标：分类（极性）用 Accuracy + F1，回归（强度）用 MAE + 皮尔逊相关系数。
- **数学本质**：**带缺失掩码的鲁棒多模态融合（分类 + 回归双任务）**，核心是"如何让模型在任一模态局部归零时仍不崩溃"，属于**不完全数据下的条件推断**。
- **主要难点**：
  1. **"局部缺失"≠"整模态缺失"**：缺失是某个模态里一段连续时序全零，而非该模态整体消失。这要求模型能定位"何时缺失"并绕开坏片段，而不是简单地"丢弃整模态"——直接套用整模态丢弃的训练/推理策略会失效。
  2. **缺失类型 × 位置 × 时长的三维影响规律**：要分别度量"哪一模态缺失""缺失发生在前/中/后段""缺失多长"对极性与强度预测的不同影响，需要做系统的消融/扰动实验设计，工作量与可解释性要求都高。
  3. **训练—测试的分布对齐**：鲁棒性须在训练中注入与附件3 同构的缺失（连续零段掩码/模态 dropout），否则训练分布与测试分布不匹配；但注入方式（随机位置、随机长度、单/多模态）必须覆盖附件3 的缺失模式，否则泛化无保证。
  4. **双任务评价口径**：极性（分类）与强度（回归）要同时达标，二者目标不同（Accuracy/F1 vs MAE/Pearson），融合权重与缺失鲁棒策略可能在两任务上表现不一致，需协调。
  5. **可复现与无标签测试**：附件3 无标签，只能靠验证集评估与消融推断规律，最终在测试集提交；模型须可复现（代码 + 参数 + 环境）。

### 【问题3分析】可解释性情感预测建模与验证
- **问题目标**：针对三模态信息完整但决策依据难追溯的场景，构建可解释性多模态情感预测模型；模型在给出极性与强度预测的同时，以可量化、可复核方式说明本次判断主要依据——不同模态在当前样本中的作用差异、起主导作用的模态、各模态中与判断密切相关的局部片段；关键证据须可对应至原始文本片段/语音时段/视觉关键帧。对附件4 测试样本推理并提交预测与解释文件。
- **已知信息**：附件4 为信息完整的三模态特征 + 配套原始视频（用于把模型给出的证据时间位置回看至原始片段）；特征字段/维度/时序组织与附件2 一致；评价同上（分类 Accuracy/F1，回归 MAE/Pearson）。
- **求解目标**：① 可解释性模型（结构/目标/训练/超参）；② 典型样本"解释卡"（预测 + 三模态作用程度 + 主要参考模态 + 关键证据定位）；③ 模态内局部片段重要性分布可视化、三模态作用差异对比；④ 附件4 全量预测与解释汇总；⑤ 验证集基础性能评价与错误归因。
- **数学本质**：**可解释多模态融合（预测 + 归因）**，归因对象有二——模态级贡献（哪个模态主导）与片段级贡献（模态内哪段最关键），需定义"作用程度""关键证据"的可量化度量。
- **主要难点**：
  1. **"可解释"必须可量化、可复核**：不能只给定性描述，要给出模态作用程度的数值（如归一化贡献比）与片段重要性分数，并能映射回原始文本/语音/视觉坐标——这本质是定义一个**可辩护的归因函数**（注意力、梯度、扰动/Shapley 等）。
  2. **解释与预测的耦合**：解释模块不能严重牺牲预测精度（题目仍评价 Accuracy/F1/MAE/Pearson），也不能"为解释而解释"导致解释与真实决策脱节；需论证解释的可信度（如扰动验证：遮掉高重要片段后预测是否显著改变）。
  3. **跨模态证据定位的坐标换算**：特征序列位置（50 或 500）须换算回原始素材时间/词位置；问题1 建立的"对齐映射记录"在此成为关键基础设施——没有它，证据定位无法回看原始片段，可解释性交付不成立。
  4. **三模态作用差异的可视化与对比**：要在样本级与集合级同时呈现视觉/语音/文本的贡献分布，并解释为何某模态在某样本主导，需设计清晰的聚合与对比口径。

---

## 三、小问逻辑关系分析

**【问题链分析】**
- 问题1 → 问题2/3：问题1 建立**特征底座与对齐规则**（标准化时序特征 + 可核验的时间映射）。问题2、问题3 的预测模型都建立在"三模态时序特征"之上；而问题3 的证据定位又**强依赖问题1 的时序对齐映射**把特征位置回译到原始素材。问题1 的规范（同一特征版本、同一输入接口）是问题2/3 正确性的前提。
- 问题2 → 问题3：二者共用附件2 训练/验证，但**任务重心不同**——问题2 研究"缺模态时仍准"，问题3 研究"不缺模态时说得清"；可把问题2 的鲁棒融合（含缺失掩码/ dropout 机制）作为问题3 的基础 backbone，再叠加解释模块；二者在验证集上都要报告基础性能，便于横向比较"加缺失鲁棒"与"加可解释"对精度的影响。
- 数据继承：附件1（原始视频 + label）→ 问题1 特征生成与 100 样本汇总；附件2（aligned/unaligned 特征 + 标签）→ 问题2/3 训练与验证；附件3（缺失测试）→ 问题2 专项推理；附件4（完整测试 + 视频）→ 问题3 专项推理与证据回看。

**【整体解题思路】**
1. **数据底座（问题1）**：确定特征版本（建议统一选 aligned_50.pkl 或 unaligned_50.pkl 之一并全程一致）；用开源工具完成文本（BERT 类）、语音（如 COVAREP/openSMILE/wav2vec）、视觉（如 OpenFace/FACET）特征提取；定义跨模态对齐到统一时间网格（如 50 位置）的重采样/截断/填充规则，保留"特征位置↔原始时间/词位置"映射日志；输出符合"覆盖完整 + 时序可核验 + 可复现"三要求的 100 样本特征文件与汇总表。
2. **鲁棒预测（问题2）**：以多模态Transformer/门控融合为 backbone，训练期注入与附件3 同构的"连续零段缺失"掩码（单/多模态、随机位置与长度）；设计缺失感知融合（如模态门控、缺失指示位、重建辅助头）；在验证集做"缺失类型×位置×时长"系统消融，量化影响规律；对附件3 推理提交。
3. **可解释预测（问题3）**：在问题2 backbone 上叠加归因机制（注意力权重/梯度×输入/扰动重要性/Shapley），输出模态级贡献与片段级重要性；建立"特征位置→原始文本/语音/视觉坐标"回译；生成解释卡与可视化；对附件4 推理提交预测与解释。
4. **统一交付**：按题目第四节要求组织论文（方法/模型/训练/指标/结果）与附件（特征文件、模型代码与参数、专项测试 CSV），严格去标识。

---

## 四、关键变量分析

| 变量类型 | 内容 |
|---|---|
| **输入变量** | 原始：100 条视频（video_id/clip_id、时长、text 转写）、label-100.xlsx（label∈[-3,3]、annotation∈{Neg,Neu,Pos}）；特征：附件2 的 text(50×768)/text_bert(3×50)/audio(50 或 500×74)/vision(50 或 500×35)，字段 id/raw_text/annotations/classification_labels/regression_labels，audio_lengths/vision_lengths；专项：附件3（带随机连续零段缺失的无标签特征）、附件4（完整无标签特征 + 原始视频）；评价口径：分类 Accuracy/F1、回归 MAE/Pearson |
| **输出变量** | 问题1：100 条样本的标准化三模态时序特征文件 + 汇总表（样本编号/模态/有效时长/特征维度/对齐粒度）+ 典型样本对齐展示 + 复现说明；问题2：情感极性 + 强度预测（附件3 CSV）+ 缺失影响规律与消融；问题3：极性 + 强度预测（附件4 CSV）+ 主要参考模态/模态作用程度/关键证据定位（回译至文本段/语音段/视觉帧）+ 解释卡 |
| **决策变量** | 问题1：特征提取工具与版本、各模态特征定义、对齐规则（统一时间网格粒度、重采样/截断/填充策略）、有效长度与填充标记；问题2：融合结构、缺失注入策略（类型/位置/长度分布）、缺失感知机制、双任务损失权重、阈值；问题3：归因方法（注意力/梯度/扰动/Shapley）、模态贡献与片段重要性的量化定义、回译映射 |
| **影响因素** | 三模态原生时间尺度差异（文本词级 vs 语音/视觉帧级）、对齐粒度选择、缺失模态类型与缺失位置/时长、模态间一致性/冲突、样本情绪强度分布、工具提取质量、训练集规模（4850）、验证集阈值选择、特征版本一致性 |
| **约束条件** | 禁止使用赛题外情感数据训练/微调/选阈值；训练/验证/专项测试须保持同一特征版本与同一输入接口；问题1 特征须覆盖完整、时序可核验、可复现；问题2 缺失为"局部连续零段"而非整模态；问题3 证据须可回译至原始片段；全部提交材料严禁出现身份信息；附件总大小 ≤50MB |

**关键变量的作用与影响**
- **对齐粒度（统一时间网格位置数）**是问题1 的"总开关"：取太小（如 50）会丢时序细节、不利证据定位；取太大增加计算且可能超出附件2 既定形状。它同时约束问题2 的输入维度与问题3 的证据定位分辨率，需在问题1 就定好并全程一致。
- **缺失掩码的三要素（类型/位置/时长）**是问题2 鲁棒性的实际驱动变量；若训练注入分布与附件3 实测缺失分布不匹配，验证集表现好但测试集会塌——必须用附件3 的"随机连续零段"结构反推训练注入设计。
- **归因函数定义**决定问题3 是否"真可解释"：注意力权重直观但可能不忠实（注意力≠重要性），梯度/扰动更稳但实现重；需选一个可辩护且可验证（扰动一致性检验）的口径，并明确"模态作用程度"的归一化方式。
- **特征版本一致性**是隐性硬约束：题目反复强调 aligned/unaligned 两套须择一并保持全程一致，跨版本混用会直接破坏可比性与可复现性。

---

## 五、初步建模方向判断

**【可能建模方向】**
1. **问题1（特征与对齐）**：
   - 文本：直接采用附件2 预计算 `text`（BERT 50×768）以保证一致；若自提则用 BERT 族编码 + 截断/填充到 50 位置。
   - 语音/视觉：用成熟开源工具（语音 COVAREP/openSMILE 或 wav2vec 派生 74 维；视觉 OpenFace/FACET 派生 35 维）提取，再按统一时间网格重采样（线性/样条插值或动态时间规整 DTW）。
   - 对齐规则：定义"锚定文本词时间戳、语音/视觉帧率重采样到 50 网格"的确定算法，输出"特征位置→原始时间/词位置"映射表；填充位置显式标记为 mask，保留有效长度。
   - 交付：100 样本特征文件 + 汇总表 + 典型样本三模态对应展示 + 工具版本/参数/日志。
2. **问题2（鲁棒预测）**：
   - Backbone：多模态 Transformer（跨模态注意力）或门控融合（Gated Multimodal Unit / TFN/MMIM 类思想）。
   - 缺失鲁棒机制（可组合）：① 训练期**模态 dropout / 连续零段掩码注入**（覆盖单/多模态、前/中/后段、不同长度）；② **缺失指示位**（把"是否缺失"作为额外通道输入）；③ **重建辅助头**（对缺失段做自重建，逼出鲁棒表征）；④ **不确定性感知融合**（参考超图条件扩散、代理驱动等近年方法，见题目参考文献 [2][5]）。
   - 评价与规律：验证集做"缺失类型×位置×时长"三维消融，报告 Accuracy/F1/MAE/Pearson 随缺失率变化曲线；对附件3 推理提交。
3. **问题3（可解释预测）**：
   - 在问题2 backbone 上加**归因模块**：模态级用门控权重/注意力聚合得模态贡献比；片段级用"梯度×输入"或扰动重要性（遮掉某段看预测变化）得片段分数。
   - 回译：用问题1 的映射表把特征位置换算回文本片段/语音时段/视觉帧，生成"解释卡"。
   - 可信度检验：对高重要片段做扰动（遮掉）验证预测显著下降，证明解释忠实。
4. **通用方法层**：题目允许开源工具与预训练模型，故重心在"流程定义 + 口径一致 + 可复现 + 系统消融/归因"，而非从零造网络；建议以**验证集双任务指标 + 消融/归因可视化**作为方法优劣的主证据。

**【判断依据】**
- 题目**不强求自提特征**，附件2 已给可直接输入的三模态特征，且强调"同一特征版本/同一接口"——因此问题1 的得分点在**对齐规则的可核验性与可复现性**，而非特征新颖度；自提特征仅作为补充或对照。
- **缺失是"局部连续零段"**而非整模态消失 → 不能套用"整模态丢弃"的旧范式，必须做"段级掩码 + 缺失指示 + 重建/不确定性"的组合，并在训练注入分布上对齐附件3。
- **可解释须可量化可回译** → 归因函数必须可辩护且可验证（扰动一致性），且依赖问题1 的对齐映射；没有对齐映射，证据定位交付不成立。
- **双任务评价（分类+回归）** → 损失需加权或分阶段，阈值在验证集选；两任务的鲁棒/解释策略要协调，避免此消彼长。
- 题目给出 10 篇近三年参考文献（含 CVPR/ICCV/ACL 2025–2026 的缺失模态、可解释、超图扩散等方法）→ 可直接借鉴其思想作为建模依据，但须自行论证与实现，且不得照搬 AI 生成方案（见题目 AI 使用规范）。

---

## 六、赛题难点总结

**【核心问题】**
在"三模态时间尺度异构、采集常致局部缺失、决策须可追溯"三重现实约束下，如何建立一套**从原始视频到标准化时序特征（可核验对齐）、再到缺失鲁棒预测、再到可解释预测**的自洽方法链，使模型既准、又稳、还说得出依据。核心矛盾是**预测性能、缺失鲁棒性、可解释性三者之间的张力**，而贯穿全题的两条数学主线是"跨模态时序对齐"与"缺失/归因下的条件推断"。

**【关键难点】**
- **数据**：三模态原生时间尺度完全不同，对齐粒度与填充规则须既保信息又保可核验；附件2 两套版本（aligned/unaligned）须择一并全程一致；100 样本覆盖完整、标签与文件严格对应是第一道门槛。
- **模型**：局部连续缺失（非整模态）要求段级掩码与缺失感知融合，旧"模态丢弃"范式失效；双任务（分类+回归）目标不一致需协调；可解释要求定义可辩护且可验证的归因函数，解释不能牺牲精度也不能脱节。
- **算法**：鲁棒性需训练—测试缺失分布对齐的注入设计；解释需模态级+片段级双层归因与坐标回译；消融/归因的可视化与对比口径需自行定义且可解释。
- **应用**：必须按题目第四节严格交付（论文逐问题回应 + 100 样本特征文件 + 专项测试 CSV + 去标识 + ≤50MB），且 AI 使用须披露、不得代写核心建模。

**【后续建模重点】**
1. **问题1 先定对齐契约**：统一时间网格粒度、重采样/截断/填充算法、有效长度与 mask 标记、"特征位置↔原始坐标"映射日志——这是问题2/3 的地基，必须一次定好全程一致。
2. **缺失注入对齐附件3 结构**：以"随机连续零段、单/多模态、前/中/后段、不同长度"为维度设计训练注入，使训练分布覆盖测试分布。
3. **双任务损失与阈值在验证集闭环**：Accuracy/F1 与 MAE/Pearson 分别选阈值与加权，报告验证集基础性能作为方法基线。
4. **归因选可验证口径**：优先梯度×输入或扰动重要性（比纯注意力更忠实），并用"遮高重要片段→预测显著下降"做忠实性检验。
5. **解释回译依赖对齐映射**：问题3 证据定位必须能落到原始文本段/语音段/视觉帧，提前打通回译链路。
6. **严格去标识与可复现**：附件代码/参数/环境说明齐备，论文披露所用开源工具版本与 AI 使用范围，避免违规。

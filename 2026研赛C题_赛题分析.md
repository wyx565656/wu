# 2026年C题 服务于脑机接口与精神性疾病诊断的脑电图计算模型 —— 赛题分析

> 分析基准：题目正文（背景、三个尺度模型、实验设计、建模问题、附录实验说明）+ 数据 VisualCogA_Task-1.mat / VisualCogA_Task-2.mat / VisualCogB_Task-1.mat / VisualCogB_Task-2.mat（MATLAB 5.0 格式，各约 8.5–8.9 MB，SampleRate=256）
> 输出要求：不套固定模型、优先问题适配性、结论仅作为后续建模方案设计依据

---

## 一、赛题背景分析

**【问题背景】**
脑电图（EEG）是非侵入式记录大脑活动的核心技术。基于 EEG 的计算模型是连接基础神经科学与临床应用的桥梁：既可以在虚拟环境中检验假说、预测难以落地的神经交互结果，也可通过个体数据调参实现个性化适配。题目给出三个空间尺度的模型谱系——**微观**（Hodgkin-Huxley / 耦合 LIF，式1）、**介观**（Wilson-Cowan 平均场，式2~3）、**宏观**（Kuramoto 振子网络与序参数，式4~5）。核心科研问题是：**如何把微观神经元活动规律与宏观头皮观测的 EEG 联系起来**。临床上，运动后 β 反弹（PMBR）减弱与精神分裂症相关，海马体信号迟缓微弱被认为是阿尔茨海默症早期指标。本题选取"视觉认知"这一可控场景：刺激 → 视网膜/LGN → 皮层（额叶区）→ 头皮 F3/Fz/F4 被观测，观测目标为 P300（刺激后约 250–500 ms 的中央-顶区正向事件相关电位）。

**【现实意义】**
EEG 未能在临床与脑机接口广泛落地的主因有二：①数据处理与解读困难（干扰强、视物相关信号极弱）；②二维头皮到三维大脑空间的映射问题（反问题非唯一，实际价值受限）。若能用"正演机理模型"替代"病态反演"，就能为 BCI 解码和神经性疾病早期诊断提供可解释、可验证的理论框架。题目的两个实验项目（项目1：已知目标位置等待出现；项目2：已知形状（左/右三角）但不知位置）恰好构成"位置预期"与"形状识别"的对照，是研究形状相关脑电特征的良好设计。

**【数学建模目标】**
用数学方法刻画三个层次：①信号层——在保留视物形状相关脑电特征的前提下完成伪影去除与有效视觉响应提取；②机理层——建立从 LGN 到皮层到头皮观测的 EEG 正演计算模型，解释左右三角刺激产生差异的机制并给出可区分的特征表示；③认知层——整合海马体与 LGN 等多来源，建立认知宏观模型并用实测数据验证。

---

## 二、问题拆解

### 【问题1分析】伪影去除、有效视觉响应提取与曲线拟合
- **问题目标**：针对实验项目1 与项目2，对给定 EEG 数据做资料预处理（去除运动、眨眼等伪影），**关键约束是：现有多种去伪影方案会在去干扰的同时破坏反映视物的脑电特征，因此必须设计"保形状特征的降噪方法"**；进而提取不同观测点位（F3/Fz/F4）的有效视觉响应并拟合相应曲线。它在整题中承担"提供干净可用的 ERP 数据"的基础作用。
- **已知信息**：四组数据（A/B 各两个 Task），`SampleRate=256`；每组 10 通道 `DataLabel=[Fz F3 F4 FzDecon F3Decon F4Decon ECG VisCue TgtAct TimeStamp]`——**通道1–3 为原始数据（可用）；通道4–6 为机器自带滤波器输出，题目明确要求避免直接使用**；通道7 ECG 用于说明记录系统正常；通道8 `VisCue`（−1 左靶 / +1 右靶）为视觉提示标记；通道9 `TgtAct`（−2 点击左侧 / +2 点击右侧）为目标应答；通道10 为时间戳。实验流程（附录）：等待视觉提示约 0.5 s → 视觉提示（右靶）约 0.2 s → 刺激消失后等待约 2 s → 点击目标。
- **求解目标**：项目1 与项目2 各自的去噪后 EEG、F3/Fz/F4 三点位的有效视觉响应波形与拟合曲线；方法需说明"如何在不破坏视物特征的前提下抑制伪影"，并给出可核验的处理记录。
- **数学本质**：**信号处理 / 盲源分离 / 时频分析 + 事件相关电位（ERP）估计**，是一个带**结构保真约束**的降噪问题（约束优化或正则化问题）。
- **主要难点**：
  1. **信噪比极低**：视物相关信号很弱而伪影很强（眨眼、眼动、肌电、工频），直接做普通滤波（如通道4–6 所示的机器滤波）会把与形状相关的波形结构一起抹掉——这正是题目设置的"陷阱"，必须用**保形约束**（如以波形形态相似度、时频能量分布、或与刺激锁时结构的一致性作为正则项）来引导降噪。
  2. **单试次 vs 平均的取舍**：试次平均能提信噪比但会平滑掉个体差异与形状相关细节；需要给出分段（epoch）提取规则与是否分条件平均的论证。
  3. **事件对齐**：必须用 `VisCue`（通道8）与 `TgtAct`（通道9）建立事件时间轴，并处理"应答不一定正确、可能未及时应答"这一题目提示带来的**试次筛选问题**（不能简单丢弃，需说明规则）。
  4. **两个项目的处理差异**：项目1 已知位置、项目2 已知形状，两者的预期效应（anticipation）与刺激响应不同，去噪与提取策略应分别设计并对比。
  5. **拟合方法选择**：P300 是 250–500 ms 的正向峰，拟合需在时域上可解释（如多高斯/样条/参数化 ERP 模板），并给出拟合优度。

### 【问题2分析】LGN→皮层→头皮的脑电计算模型与左右三角差异机制
- **问题目标**：建立脑电计算模型，说明从 LGN 到皮层再到头皮观测 EEG 的机理；用模型解释"左、右三角刺激产生的脑电信号差异"的形成机制；并给出能**区分左右三角**视觉刺激响应的特征表示。题目给出重要提示：人脸等物体的识别是通过检测特定空间方式排列的特征实现的（图9：某神经元仅对"轮廓包裹双眼 + 双眼在嘴部上方"的结构产生强响应，简化图标同样能引发响应）。
- **已知信息**：三个尺度的参考模型（LIF、Wilson-Cowan、Kuramoto 及其序参数）；从视网膜→丘脑 LGN→额叶某区域→头皮的路径描述；电极位置示意（F3/Fz/F4，F=额叶、C=中央、P=顶叶、O=枕叶、Z=中线、奇偶分左右半球）；皮质偶极层示意（大量神经元群信号的宏观源）；题目明确指出**反问题非唯一，实际应用价值不大**，并指出现有宏观模型（如 Kuramoto）引入的序参数可反映同步后的 EEG，但**未考虑神经元的空间分布**，且我们无法给出刺激引起的神经元空间分布。
- **求解目标**：① 机理说明（LGN→皮层→头皮的正演链条）；② 左右三角差异的机制解释；③ 可区分左右三角响应的特征表示（需要能定量给出可分性）。
- **数学本质**：**电生理正演建模（源 → 传导介质 → 头皮电位）+ 神经元群动力学 + 特征检测型感受野建模**；核心是从"刺激几何结构"到"源分布差异"再到"观测差异"的**确定性映射链**，而非病态反演。
- **主要难点**：
  1. **跨尺度桥接**：题目明确指出"迄今为止还没有广泛接受的神经元群体集体活动的数学理论"，因此必须自己给出从微观（HH/LIF）到宏观（序参数/偶极子）的桥接假设，并保证该假设不依赖"已知神经元空间分布"这一不可得信息——可能的出路是**用刺激的几何结构构造等效源分布**（如三角朝向决定响应神经元群的朝向选择性分布）。
  2. **头皮电位正演**：需要引入"皮质偶极层"模型（电流源密度 → 体积导体中的电位），并处理二维头皮观测与三维源的关系；简化路线是多层同心球头模型（脑–脑脊液–颅骨–头皮）解析/半解析求解，再叠加 F3/Fz/F4 的电极位置与敏感度。
  3. **序参数的空间分布修正**：题目指出 Kuramoto 序参数未考虑神经元空间分布，而不同分布给出不同序参数，因此需要提出**带空间结构的序参数定义**（如按皮层区域分块求和、或引入空间核加权），这正是本题的开放创新点。
  4. **左右三角的差异来源**：需要论证差异来自"朝向选择性神经元的响应强度不同/激活区域偏移/同步程度不同"，并说明如何体现到头皮电位上（例如 Fz 与 F3/F4 的对称性破缺）。题目的图9 提示可用**结构化特征检测器**（轮廓 + 内部特征位置的层级组合，类似 HOG/Gabor 池化/COSFIRE）来刻画"三角形朝向选择性"，使特征表示可计算、可验证。
  5. **可区分特征的可验证性**：必须给出定量可分性指标（如两类条件下的效应量、AUC、t 检验 p 值、可分性随时间的曲线），否则只是定性描述。

### 【问题3分析】认知宏观模型建立与验证
- **问题目标**：截取与视觉认知相关联的信号（**终点可能是应答响应之前的大约 100 ms**）；借助问题2 建立的脑电信号形成机制；考虑信号可能同时来源于海马体与 LGN 等；建立**认知的宏观模型**，并用题目提供的脑电信号验证该模型。并特别提示：**应答不一定正确，也可能包括没有及时应答**。
- **已知信息**：问题1 的干净 ERP 与拟合曲线；问题2 的机理模型与特征表示；从"看见视物（阶段1 直接视觉刺激响应）→ 识别视物（阶段2 从海马区对照视物）→ 应答（阶段3）"的三阶段划分（题目说明只考虑前两阶段）；`TgtAct` 通道提供的行为应答与时序；四组数据的两个实验项目差异。
- **求解目标**：认知宏观模型（含多来源、时间延迟、应答的相关性）；模型对实测 EEG 的验证结果（定量误差/相关/一致性），并说明模型解释力与局限。
- **数学本质**：**多源耦合的动力学系统辨识 + 模型验证**（可归入状态空间/延迟微分方程/神经场模型 + 参数估计与模型选择）。
- **主要难点**：
  1. **多来源叠加的不可分辨性**：海马体与 LGN 信号在头皮上叠加，需要引入**可分辨的时间延迟结构**（海马比对阶段应晚于直接刺激响应）或空间模式差异来分离二者贡献，否则模型不可辨识。
  2. **"阶段2 识别"缺乏直接观测量**：只能从刺激后到应答前的信号形态与幅度推断，需要用时间窗切分与分段建模（阶段1 早成分、阶段2 晚成分/持续成分）。
  3. **行为数据的噪声**：应答错误或未及时应答的试次必须给出筛选/加权规则，且要论证该规则不会引入选择偏差（题目特意提醒，属于考察点）。
  4. **模型验证口径**：验证什么（波形形态？峰值潜伏期？左右条件差异方向？与行为的相关性？）需要明确，并给出可复现的验证流程与统计检验。
  5. **样本量有限**：仅四组数据，个体差异大，模型自由度必须受控（避免过拟合），建议用**少参数可解释模型 + 交叉验证**。

---

## 三、小问逻辑关系分析

**【问题链分析】**
- 问题1 → 问题2：**数据层到机理层**。问题1 输出的"保形降噪后的 ERP 与三电极响应曲线"是问题2 机理模型的**验证基准**；同时问题2 需要"左右三角条件下的差异"，而该差异只能从问题1 的处理结果中提取。问题1 的保形降噪方法若破坏形状相关特征，问题2 的可分特征将无法成立——**问题1 是问题2 的成败前提**。
- 问题2 → 问题3：**机理层到认知层**。问题2 给出"源分布差异 → 头皮电位差异"的正演机制与特征表示；问题3 直接借用该机制，并在此基础上叠加海马体来源与"识别（对照）"过程，把"单次刺激响应"扩展为"刺激—识别—应答"的认知过程模型，用应答前约 100 ms 的时间窗作为关键考察区间。
- 问题1 与问题3 之间也存在间接依赖：问题3 的信号时间窗截取与试次筛选规则，应复用问题1 的事件对齐与筛选逻辑，保证口径一致。
- 三问共同构成 **"数据 → 机理 → 认知"** 的纵向递进，且每一问都要对四组数据（项目1/项目2 × 两个样本集）分别作答，形成 2×2 的对照结构。

**【整体解题思路】**
1. **数据准备**：加载四个 `.mat`，按 `DataLabel` 定位通道；用 `VisCue` 建立刺激事件时间轴、用 `TgtAct` 建立应答时间轴；按"刺激前基线—刺激后 800~1000 ms"切分 epoch；屏蔽通道4–6。
2. **保形降噪（问题1）**：预先设定"必须保留的结构"作为约束——例如 P300 的时间尺度（>100 ms 的慢波结构）、左右条件差异的方向性、跨试次锁时一致性；在此约束下抑制眼动/肌电/工频（多通道 ICA/伪迹子空间剔除、小波或 EMD 分层阈值、低秩+稀疏分解、自适应滤波等），并用"降噪前后形状相似度 + 可分性保留率"做双重评价。之后做分电极、分条件的 ERP 估计与参数化拟合。
3. **正演机理模型（问题2）**：
   - 层级1：**刺激几何 → 特征检测响应**。用朝向选择性感受野/结构化特征检测（Gabor 金字塔、HOG-like 池化、轮廓+内部特征位置组合）计算左右三角的激活强度图；
   - 层级2：**响应神经元群 → 源分布**。由激活强度图构造等效源（偶极层）分布，避免依赖不可得的真实神经元空间分布；
   - 层级3：**源 → 头皮电位**。多层同心球头模型 + 电极敏感度，得到 F3/Fz/F4 的模拟电位（含时间常数/延迟）；
   - 层级4：**序参数**。给出带空间结构的序参数定义（解决题目指出的"Kuramoto 未考虑空间分布"问题），把源分布差异映射为可比较的标量差异。
   - 输出：差异机制解释 + 可区分特征（模拟与实测同时计算的可分性指标）。
4. **认知宏观模型（问题3）**：以"直接刺激响应项（LGN/皮层通路，短延迟）+ 识别/对照项（海马通路，较长延迟与较慢时间常数）"构成可解释的双（多）通道模型，输入为问题2 的刺激特征、输出为头皮电位；用四组数据（不同项目、不同应答条件）估计少量参数并验证；对应答错误/缺失试次给出筛选与鲁棒性检验。

---

## 四、关键变量分析

| 变量类型 | 内容 |
|---|---|
| **输入变量** | 四组 EEG 数据（每组 10 通道：Fz/F3/F4 原始、FzDecon/F3Decon/F4Decon（禁用）、ECG、VisCue(±1)、TgtAct(±2)、TimeStamp）；采样率 256 Hz；实验设计（项目1 位置已知 / 项目2 形状已知，左/右三角，右靶提示）；电极位置（F3/Fz/F4 及其解剖学含义）；刺激时序（提示 ~0.5 s、刺激 ~0.2 s、间隔 ~2 s）；附录给出的流程与图9 的结构化特征提示 |
| **输出变量** | 去噪后 EEG 与各电极有效视觉响应波形、拟合曲线；左右三角差异特征（效应量/AUC/可分性曲线）；模拟的头皮电位、源分布、序参数；认知模型输出与被试行为的一致性；各类误差与统计检验量 |
| **决策变量** | 降噪算法与参数（源数、阈值、正则权重、保形约束形式）；试次筛选与分段规则；拟合函数族与阶数；特征检测器形式（尺度/朝向/池化方式）；源分布构造规则；序参数的空间核与分块方式；认知模型的通道数、延迟与时间常数、耦合结构 |
| **影响因素** | 伪影类型与强度、视物相关信号强度（很弱）、刺激条件（左/右、项目1/项目2）、电极位置与参考（通道7 作参考）、个体差异、试次数量与应答正确率、模型自由度过大导致的过拟合 |
| **约束条件** | 不得使用通道4–6；不得替换/修改数据与标签；必须保留样本完整性；只能基于题目数据做训练与验证；模型必须能解释 LGN→皮层→头皮的机理而非单纯数据拟合；反问题非唯一 → 不宜依赖病态反演；输出需可核验（可复现的处理流程与参数） |

**关键变量的作用与影响**
- `VisCue` 与 `TgtAct` 是**唯一的时序锚点**：前者定义刺激起点（决定 P300 时间窗），后者定义认知窗口的终点（"应答前约 100 ms"），二者的对齐质量直接决定问题1、3 的全部结论。
- `通道4–6（Decon）` 是**负向约束变量**：题目明令禁用，而它也是最好的"反面案例"——可用于论证"机器滤波破坏了哪些结构"，是问题1 方法对比的天然对照。
- "左右三角朝向"是**差异的自变量**：机理模型的全部差异必须最终归因到它；特征表示的优劣应以"左右可分性"作为量化评价，而非只看波形是否好看。
- **序参数 `r(t)` 的空间定义**是问题2 的创新点所在：题目已指出原定义忽略空间分布，因此对 `C_np`/空间核的建模方式会直接决定模型能否解释"相同同步度但不同源分布"的差异。
- **试次筛选/应答正确性**是潜藏的混杂变量：若按应答正确的试次做统计，会引入选择偏差，必须在问题1 与问题3 中统一处理规则。

---

## 五、初步建模方向判断

**【可能建模方向】**
1. **保形降噪（问题1）**：
   - 候选方法族：ICA/伪迹子空间剔除（基于眼动/肌电模板分量）、小波或 EMD 分层阈值、低秩+稀疏分解（RPCA 类，低秩=稳定背景、稀疏=伪影）、自适应噪声抵消、SSA/卡尔曼滤波，以及多试次 ERP 模板估计。
   - **保形约束的构造**（本题关键，建议显式写出）：以"波形形状相似度（相关系数/形状距离）+ 时频结构保持 + 左右条件可分性保留"作为约束或正则项，把"去噪"写成"在结构损失不超过阈值的前提下最小化噪声能量"的约束优化，并对阈值做敏感性分析。
   - 拟合：参数化 ERP 模板（多高斯/α 函数/样条）拟合 P300 及其前后成分。
2. **正演机理模型（问题2）**：
   - **结构化特征检测 → 源分布**：用 Gabor/朝向选择性滤波 + 层级池化（HOG / COSFIRE 思路，正对应题目图9 提示）计算左右三角的激活强度，构造等效偶极层源分布；避免依赖不可得的真实神经元空间分布。
   - **源 → 头皮电位**：多层同心球头模型 + 电极位置敏感度，实现"正演"而非病态反演（与题目对反问题的批评一致）。
   - **微观到宏观的桥接**：以 LIF/Wilson-Cowan 描述神经元群响应的时间常数与非线性饱和，用 Kuramoto 序参数描述同步；对序参数做**空间结构修正**（分块/空间核加权求和），提出修正后的可计算定义。
   - **可区分特征**：以左右条件在模拟与实测中的可分性（效应量、AUC、时间分辨可分性曲线）作为特征优劣的判据。
3. **认知宏观模型（问题3）**：
   - **双/多通道延迟结构模型**：刺激直接响应项（短延迟、快时间常数）与识别/海马对照项（长延迟、慢时间常数），可写成延迟微分方程或状态空间形式（少量可解释参数）。
   - **参数估计与验证**：非线性最小二乘/贝叶斯参数估计，交叉验证；验证口径包括波形形态、潜伏期、左右条件差异方向以及与行为应答的一致性；对应答错误/缺失试次做鲁棒性检验。
4. **统一评价体系**：为三问分别设定可核验指标（问题1：去噪前后形状相似度与可分性保留率；问题2：模拟与实测的波形相关性、左右可分性；问题3：模型预测与实测的误差、与行为的相关性、统计检验）。

**【判断依据】**
- 题目**明确否定了纯反问题路线**（"反问题的非唯一性及大脑中电信号的传输复杂性，反问题方法的实际应用价值并不大"）→ 应选择**正演建模 + 机制假设**的路线，这与我们"刺激几何 → 源分布 → 头皮电位"的链条一致。
- 题目**明确指出了现有宏观模型的缺陷**（Kuramoto 序参数未考虑神经元空间分布，且空间分布不可知）→ 这是题面直接指明的创新口，模型必须回应"如何在不依赖真实空间分布的前提下得到有意义的序参数"。
- 题目**用图9 的神经生理证据作为提示**（结构化特征 + 特定空间排列触发响应）→ 强烈暗示问题2 的特征表示应采用**基于特征检测器的层级表示**，而不是黑箱全连接模型。
- 题目**强调样本完整性、不得替换数据、需可复现**，且只有四组数据 → 模型必须**少参数、可解释、可交叉验证**，不宜使用大规模深度学习端到端模型作为主方法。
- 问题3 明确给出**时间窗线索（应答前约 100 ms）** 与**应答可能错误/缺失的提示** → 说明评分关注"事件窗切分的严谨性"与"行为数据处理的稳健性"，这两点应作为独立小节重点论证。

---

## 六、赛题难点总结

**【核心问题】**
核心矛盾是：**在强伪影、极弱目标信号、二维头皮观测与三维皮层源之间关系病态的条件下，如何建立一条"刺激几何 → 神经元群响应 → 源分布 → 头皮电位"的确定性、可验证、可解释的映射链**，并使之能区分左右三角刺激、解释认知过程。其难点不在单点算法，而在**跨尺度桥接的合理性与可验证性**。

**【关键难点】**
- **数据**：视物相关信号极弱而伪影极强；仅有 4 组数据（2 项目 × 2 数据集），样本量小、个体差异大；关键通道4–6 被禁用；应答存在错误与缺失，试次筛选易引入偏差。
- **模型**：微观–介观–宏观三层次的桥接缺乏公认理论；序参数原定义忽略空间分布而空间分布不可知；头皮电位的正演需要介质与几何假设；"识别（阶段2）"没有直接观测量，只能间接推断。
- **算法**：降噪必须在"去伪影"与"保形"之间取得可量化的平衡，普通滤波会破坏特征；左右差异的可分性需要定量指标支撑；模型参数必须少而可辨识。
- **应用**：结论需要能落到 BCI 解码或疾病早期诊断的语言上（如"用该特征可实现左右三角解码"、"海马通路指标可用于早期筛查"），而不能停留在波形描述。

**【后续建模重点】**
1. **先打通数据链**：正确解析 `.mat`（MATLAB 5.0 格式）、正确读取 `DataLabel` 对应通道、用 `VisCue`/`TgtAct` 建立事件时间轴，并抽样核验通道量纲与采样率假设（SampleRate=256）。
2. **把"保形"写成显式的约束或正则项**，并设计"形状相似度 / 可分性保留率"双指标来证明保形成功，同时用通道4–6 作为反面参照。
3. **机理模型采用正演路线**：刺激几何 → 朝向选择性特征检测 → 等效源分布 → 多层球头正演 → 电极电位；并在其中嵌入"带空间结构的序参数定义"以回应题目指出的模型缺陷。
4. **为每一问建立可核验的定量评价**：问题1（去噪前后结构相似度、可分性保留率、拟合优度）、问题2（模拟与实测波形相关、左右可分性）、问题3（预测误差、与行为应答的一致性、统计检验），并把它们提前确定下来，作为后续模型选择的依据。
5. **统一口径**：试次筛选规则、时间窗定义（刺激起点、应答前 100 ms）、分条件平均方式，在问题1 与问题3 中必须完全一致，避免结论不可比。

# 2026年D题 山区洪涝灾害下无人机运输与通信协同优化 —— 赛题分析

> 分析基准：题目正文 + `数据/无人机应急物资运输基础数据/`（调度中心与服务区、物资需求与配送时限（含逐箱货箱清单）、运输无人机数据、中继无人机数据、通信链路参数 共5个 xlsx）+ `数据/镇龙乡地理空间数据/`（30米DEM 的 .mat/.tif、村镇点位、水体、水系、道路的 csv/mat）+ `结果提交模板.xlsx`（Q1_单点组批、Q2_运输架次、Q2_逐箱交付、Q3_中继架次、Q3_通信保障、Q4_分区配置 六个工作表）
> 输出要求：不套固定模型、优先问题适配性、结论仅作为后续建模方案设计依据

---

## 一、赛题背景分析

**【问题背景】**
2026 年 7 月台风"美莎克"造成广西横州市镇龙乡严重洪涝：山洪、落石、塌方多方向阻断道路，一度断路、断电、断网，全乡约 8000 人受困。灾后早期需要把饮用水、应急食品、医疗物资、生活卫生用品尽快送达，而地面车辆难以进入。无人机受道路影响小，成为应急运输的重要补充；同时运输无人机在爬升、巡航、下降与物资交接全过程必须保持指挥、遥测与交接确认链路，当固定通信设施受损或山体遮挡时，需要**空中中继无人机**在合适位置与时段提供临时通信保障。

**【现实意义】**
这是"运输——通信——资源"三位一体的真实应急调度问题：物资投送不仅取决于能否飞抵，还受载荷、水平距离、爬升下降高度、沿线地形净空、能源储备、任务时限、设备周转共同约束；而通信保障又反过来限制飞行剖面与调度窗口。题目还进一步要求做**任务分区与资源配置**（灾后救援常需划分相对独立的执行单元），并核算资源冗余与库存缺口——直接对应应急指挥中的"兵力/装备分配"决策。

**【数学建模目标】**
用数学方法刻画：① 单点往返运输能力与不可拆货箱的组批；② 异构无人机多点多架次的运输调度（含共享电池充放电周转与时间窗）；③ 运输与中继的**联合调度**（通信链路可用性依赖三维位置与地形遮挡，且需全程连续）；④ 任务分区的资源需求、冗余与缺口评估。本质上是**在能耗、时限与资源周转约束下的异构车辆路径与资源受限调度问题，并耦合一个几何/链路预算层面的通信可行性问题**。

---

## 二、问题拆解

### 【问题1分析】单点往返运输能力与货箱组批方案
- **问题目标**：不考虑实体无人机与共享电池调度，研究单服务区直接往返（`O01→Si→O01`，同一服务区可多架次、不得跨服务区组批）的运输能力与货箱组批。
- **已知信息**：调度中心 O01 与 15 个服务区坐标（xlsx）；30 m DEM；三机型参数（最大载货质量、载具体积、航程相关参数、爬升/巡航/下降速度、电池可用能量、返航安全余量 ρ、充电相关）；80 个不可拆货箱的质量与体积清单；附录2 的**航段时间与能耗规则**：任意两节点间以水平直线为航段，巡航海拔取航段经过 DEM 像元的**最高地面高程以上 50 m**；O01 作业高度取地面海拔，服务区取地面海拔 +30 m；多服务区连续访问时每次投送后从 30 m 重新爬升；等效航程 `L_g(q)=L_g0-(L_g0-L_gF)(q/Q_g)^{3/2}`；航段能耗 = 水平航程能耗 + 爬升附加能耗（下降附加能耗按 0 计）；返航安全余量约束 `E_p^T ≤ (1-ρ_g)E_g^use`。
- **求解目标**：① 三种机型在**不同服务区**执行单点往返任务时的最大安全载荷（逐服务区、逐机型）；② 满足载质量、装载体积、返航安全能量余量的货箱组批方案；③ 在完成全部交付前提下，对往返架次数、总运输能耗、累计作业时间做优化并说明指标间权衡/优先关系；④ 讨论返航安全余量变化对最大安全载荷与组批结果的影响。
- **数学本质**：**解析计算（最大载荷的可达性求根）+ 三维/二维装箱（组批）+ 多目标组合优化**。
- **主要难点**：
  1. **最大安全载荷不是简单常数**：因为能耗依赖载荷（`L_g(q)` 非线性、`q^{3/2}` 项），而爬升高度又依赖航段的 DEM 最高点 → 必须对每个 (机型, 服务区) 组合求解"载荷—能耗—返航余量"的隐式方程（求根/不动点迭代），并处理**载荷分阶段变化**（去程满载、返程空载或部分剩余）。
  2. **巡航海拔由"航段沿线 DEM 最高像元"决定**：这意味着航段属性是**逐航段的非加性量**，不能预先用两节点连线简化；需要做地形线采样（DEM 栅格沿线的极大值提取），是本题第一处"数据必须真正用起来"的地方。
  3. **组批是三约束装箱**：载质量、装载体积、能量（因为载荷越重能耗越高，组批结果**反过来影响是否满足返航余量**），三者耦合，不能分别处理。
  4. **指标权衡**：架次少（合批多）→架次数与累计作业时间少但单架次重载能耗高；需要给出明确的权衡表述与优先关系论证，这是评分点。
  5. **灵敏度分析**：ρ_g 变化会同时改变最大安全载荷与组批粒度，需给出单调性/临界值分析。

### 【问题2分析】异构无人机多点多架次运输调度
- **问题目标**：不考虑通信保障，研究异构运输无人机的多点多架次调度：每个架次可访问一个或多个服务区并在交付后返回 O01；联合确定**货箱组批、服务区访问顺序、运输机型、具体执行无人机、共享电池分配、各架次开始时刻**。
- **已知信息**：问题1 全部内容 + 三种机型与共 8 架实体运输无人机 + 配套共享电池组（共享电池总数含实体无人机初始使用与备用周转；同机型可跨实体无人机调度、不同机型不可混用）+ 期望送达时间/首批截止时间（医疗物资有期望送达时间要求，首批保障货箱有首批截止时间要求，其余物资的期望送达时间用于衡量配送及时性）+ 电池充电周转规则（两阶段等效充电模型：SOC<90% 快速阶段占等效完全充电时间 65%，90%–100% 慢速阶段占 35%，按 SOC 增量线性折算；同一资源任务占用与充电时段不得重叠，不同资源可并行充电）。
- **求解目标**：① 综合配送及时性、全部任务完成时间（所有运输无人机完成最后一个架次并返回 O01 的最晚时刻）、运输能耗、架次数进行优化，并说明权衡关系；② 给出运输路线、架次安排、逐箱送达时刻以及无人机与电池资源使用情况，并检验方案资源可行性。
- **数学本质**：**异构车队车辆路径问题（HFVRP/VRPTW）变体 + 能源与电池资源受限调度（含充放电时间轴）**，目标为多目标。
- **主要难点**：
  1. **能耗模型破坏经典 VRP 的可加性**：载荷随投送递减且能耗非线性，且航段爬升高度依赖 DEM → 路径的能耗必须逐航段、逐载荷状态累计，不能预计算"距离矩阵"，使得经典 VRP 求解器无法直接套用。
  2. **共享电池使问题变成"路径 + 资源时间轴"双层耦合**：电池是独立于无人机的资源，需要分配、需要充电、占用与充电不可重叠 → 调度的可行性依赖时间轴，而非仅依赖路径长度。这是本题与标准 VRP 最大的区别。
  3. **时限结构多层**（首批截止、医疗期望送达、其他物资期望及时性）→ 需要设计"及时性"的量化方式（是否按时/延误惩罚/服务水平），并论证取舍。
  4. **多目标权衡**：及时性 vs 完成时间 vs 能耗 vs 架次数，四者方向不一致（如为了及时性可能牺牲能耗与架次数），需给出明确的权衡分析与优先关系。
  5. **可行性检验**：需逐架次核验载荷/体积/返航余量、逐时核验电池 SOC 与充电时间轴、逐箱核验送达时刻是否满足时限——检验本身是一套独立的工作量。

### 【问题3分析】通信约束下的运输与中继联合调度方案
- **问题目标**：运输无人机在爬升、巡航、下降及物资投送阶段**均应保持连续通信**；与固定网关 G01 直连不可用时，可由空中中继无人机提供保障。联合确定货箱组批与访问顺序、运输无人机及共享电池安排、各架次开始时刻，以及**中继无人机的悬停位置、飞行海拔、服务时段与能源组件分配**。中继悬停位置须在给定 DEM 覆盖范围内，悬停离地高度不超过附件规定上限。
- **已知信息**：问题2 全部内容 + 通信系统（固定网关 G01 设于 O01，其端点位置由 O01 地理坐标、地面海拔、网关天线离地高度确定）+ 通信链路参数（载波频率 2400 MHz、收发功率与增益、系统损耗、接收灵敏度与衰落裕量、地形遮挡附加损耗 `L_obs`、中继参数）+ 附录3 的完整判定规则：
  - 地形遮挡判定 `b_ijt`（视线与 30 m DEM 是否相交）；
  - 接收门限 `P_th = P_sens + M`；方向 `a→b` 的最大允许总传播损耗 `L_max = P_t + G_t + G_r - L_sys - P_th`；**双向链路门限取两方向较小值** `L_max^{a↔b}=min(...)`；
  - 自由空间损耗 `L_FSPL = 32.45 + 20log10 f + 20log10 D`（f 以 MHz、D 以 km 计）；总损耗 `L_path = L_FSPL + L_obs·b`；
  - 链路可用性 `A_ijt = 1[L_path ≤ L_max]`；
  - 通信状态：直连可用→直连；否则"运输无人机—中继—G01"两段**同时可用**→中继；其余→中断；**不允许中继间多跳**；任一时刻只能由 G01 或一架中继保障；中继需满足返航电量要求与固定准备时间、建链时间、架次周转时间。
- **求解目标**：① 综合物资配送及时性、联合任务完成时间（所有运输与中继完成最后架次并返回 O01 的最晚时刻）、运输与中继总能耗、两类无人机使用架次数进行优化并说明权衡；② 给出运输与中继联合调度方案，说明货箱交付、资源使用与通信保障情况。
- **数学本质**：**带时变、空间依赖的连通性约束的联合调度问题**（运输调度 + 几何选址/航迹 + 时序覆盖），底层含一个**几何可行性/链路预算判定子系统**。
- **主要难点**：
  1. **通信约束是时变的连续约束**：链路状态取决于两端点的三维位置（运输无人机随时间移动、中继固定悬停），且整条航段（爬升—巡航—下降—投送）都必须可用 → 需要**对时间上的每一点做覆盖判定**，而不是只看端点。这是"连续约束离散化"的典型难点。
  2. **中继悬停位置是连续决策变量**，且与运输路径耦合：位置好→覆盖广但可能飞得更远更耗能；位置差→需要更多架次中继或运输改线。形成"运输—通信"双层/联合优化。
  3. **地形遮挡计算的实现成本**：需要对每对端点的视线做 DEM 栅格相交检测（可在大规模场景下成为计算瓶颈），必须设计高效的视线遮挡判定（沿视线采样 + 栅格化）与缓存策略。
  4. **双向门限与两段链路同时可用的组合判定**：接入链路与回传链路必须同一时刻同时可用，且每一段的判定是双向取小 → 判定逻辑复杂，容易实现错误，需设计单元测试。
  5. **中继能源组件与架次周转**：中继的固定准备时间、建链时间、架次周转时间与能源组件充电周转同样构成资源约束，使联合调度的时间轴更复杂。

### 【问题4分析】救援任务分区与资源配置优化方案
- **问题目标**：以问题3 的联合调度方案为基础，把 15 个服务区分别划分为 **2 个和 3 个任务组**（分区方案由参赛者确定，每个服务区必须且只能属于一个任务组，每组至少包含一个服务区）；**保持问题3 已确定的货箱组批、访问顺序、运输与中继任务安排及通信保障关系不变**；若同一运输架次涉及多个服务区，这些服务区必须划入同一任务组。各组仅承担本组任务，**独立核算**所需运输无人机、共享电池、中继无人机和中继能源组件数量，**执行期间资源不得跨组调配**。比较两种分区的资源配置规模、资源冗余、组间工作量均衡与**与现有库存之间的资源缺口**。
- **已知信息**：问题3 的完整方案（架次、服务区归属、通信保障关系）；各类资源的现有库存（来自附件数据）；`结果提交模板.xlsx` 的 `Q4_分区配置` 工作表列结构。
- **求解目标**：① 2 组与 3 组分区的任务分区及资源配置方案；② 从资源规模、冗余、组间均衡、缺口四方面比较两种分区方式的特点；③ 若所需资源超库存，给出缺口及原因。
- **数学本质**：**带约束的分区/聚类（方案固定条件下的资源需求分解）+ 多目标评价**，本质是"把既有调度方案按资源需求做可行切分，并评估其资源效率"。
- **主要难点**：
  1. **分区受架次结构约束**：架次涉及多服务区则必须同组 → 分区实际是在"架次—服务区"的关联图上做连通性/超图聚类，而不是对 15 个点自由聚类；必须先构造关联结构。
  2. **资源独立核算的口径**：无人机的架次冲突、电池的充放电时间轴、中继的悬停时段，在分组后**可能出现新的资源冲突**（原方案可跨组共用，分组后不能），需要重新检验时间轴可行性——如果分组导致冲突，需要说明处理方式（题目要求"保持安排不变"，因此更可能是暴露冲突并如实报告）。
  3. **资源冗余与均衡的量化指标需要自行定义**（如峰值并行需求/库存比、资源利用率、组间工作量的方差/极差/Gini 系数），定义的可解释性直接影响得分。
  4. **缺口归因**：超出库存时的原因分析要能落到"分区是否切断了可共享的架次结构""某组集中了端部/远距离服务区"等结构性原因上。

---

## 三、小问逻辑关系分析

**【问题链分析】**
- 问题1 → 问题2：问题1 建立**单机能力边界**（最大安全载荷、组批规则、能耗与返航判据），这是问题2 中"每个架次能装什么、能飞多远"的**可行性基础**；问题2 在此之上放宽为多点多架次，并引入机型选择、实体无人机与共享电池资源。
- 问题2 → 问题3：问题3 **完全继承**问题2 的运输调度框架，只新增通信保障约束与中继决策；问题2 的无通信最优方案可作为问题3 的性能上界（对比参考），二者差异即"通信约束的代价"。
- 问题3 → 问题4：问题4 是问题3 方案的**资源分解与评估**，明确要求"保持问题3 已确定的安排不变"，只改变资源归属与核算方式，是典型的"给定方案做分区敏感性/资源配置评审"。
- 数据继承关系（题目要求论文中交代）：坐标与 DEM → 全题；箱清单与时限 → 问题1 起；机型与电池参数 → 问题1 起（电池周转在问题2 起生效）；中继与链路参数 → 问题3 起；问题3 方案 → 问题4。

**【整体解题思路】**
1. **数据底座**：解析 5 个 xlsx 得到节点坐标、箱清单、机型/中继/链路参数与库存；解析 DEM（.mat/.tif）与村镇点位，实现"给定两节点 → 巡航海拔（沿线 DEM 最高点 +50 m）→ 爬升/下降高度 → 航段时间与能耗"的计算内核，并做单元测试。
2. **问题1**：对每种（机型 × 服务区）做"载荷—能耗—返航余量"求根，得最大安全载荷表；再在载质量+体积+能量三约束下做组批（贪心/首次适应递减 + 局部改进），然后以架次数、能耗、累计作业时间为目标做多目标权衡分析，并做 ρ_g 灵敏度扫描。
3. **问题2**：构建"服务区—箱"与"架次—路线"的两级模型。由于能耗非加性，采用**构造式启发式（种子—扩展：从 O01 出发按地理邻近与时限紧迫度插入服务区）+ 大邻域搜索（LNS/ALNS）** 改造路线与组批；在适应度中嵌入电池 SOC 时间轴可行性与时限满足度；多目标用加权或 ε-约束，输出 Pareto 集并选代表解。
4. **问题3**：实现通信判定内核（视线地形遮挡 + FSPL + 双向门限 + 两段同时可用），把"连续时间连续通信"通过**航段离散采样（分段线性化）+ 关键点判定**转为可计算约束；中继位置用**候选位置枚举（DEM 网格降采样）+ 局部精细搜索**，与运输路径做**交替优化/联合 LNS**（运输固定则优选位置，位置固定则优选路线，迭代直到收敛）；能耗中新增中继飞行与悬停通信能耗。
5. **问题4**：先由问题3 方案提取"架次—服务区"关联结构，在其上做满足"架次不可分割"的 2 组/3 组划分（可用带约束聚类/枚举-启发式，服务区仅 15 个，可枚举或分支界定部分方案）；对每组独立重算资源需求与时间轴，定义规模/冗余/均衡/缺口四类指标并做对比。
6. **统一提交**：按 `结果提交模板.xlsx` 的六个工作表组织结果，逐表核对列名与口径（这是硬性交付要求）。

---

## 四、关键变量分析

| 变量类型 | 内容 |
|---|---|
| **输入变量** | 场景：1 个调度中心 O01 + 15 个服务区坐标与地面海拔；30 m DEM；80 个不可拆货箱（质量、体积、所属服务区、时限属性）；时限：首批截止时间、医疗期望送达时间、其他期望送达时间；运输参数：3 种机型 × 8 架实体无人机、共享电池组（数量、可用能量、等效完全充电时间）、机型最大载货质量与体积、空载/满载标准航程、爬升/巡航/下降速度、水平与爬升能耗系数、返航安全余量 ρ；中继参数：中继无人机数量、能源组件（数量、能量、充电时间）、悬停离地高度上限、固定准备/建链/架次周转时间、悬停与通信附加功率；链路参数：载波频率 2400 MHz、发射功率与天线增益、系统损耗、接收灵敏度、衰落裕量、地形遮挡附加损耗；作业高度规则（O01 取地面、服务区取地面 +30 m、巡航取沿线 DEM 最高点 +50 m） |
| **输出变量** | 各机型逐服务区**最大安全载荷**；货箱组批方案（箱→架次）；架次计划（路线/服务区顺序、机型、实体无人机、电池、开始与结束时刻、逐航段载荷）；逐箱送达时刻；任务完成时间与及时性指标；总运输能耗与中继能耗；架次数；电池与能源组件的 SOC 时间轴；中继悬停位置/飞行海拔/服务时段/能源组件分配；通信状态序列（直连/中继/中断）；分区方案与各组资源需求、冗余、均衡度、缺口 |
| **决策变量** | 问题1：箱→架次分组（组批）；问题2：组批 + 访问顺序（路线）+ 机型 + 实体无人机 + 共享电池分配 + 各架次开始时刻；问题3：以上全部 + 中继悬停位置（连续二维）+ 飞行海拔 + 服务时段 + 中继能源组件分配；问题4：15 个服务区→2/3 个任务组的划分 |
| **影响因素** | 航段沿线 DEM 最高点（决定爬升高度与能耗）、载荷质量（非线性影响航程与能耗）、服务区与 O01 的地理分布、时限紧迫度、往返安全余量 ρ、电池数量与充电时间（周转瓶颈）、并行任务对电池的竞争、通信链路的遮挡与距离、中继位置与覆盖范围、货箱体积利用率、路径上的投送顺序（载荷递减曲线）、分区方式导致的资源不可共享 |
| **约束条件** | 货箱不可拆分且每箱只安排一次；载质量与装载体积上限；返航安全余量 `E_p^T ≤ (1-ρ_g)E_g^use`；实体无人机与共享电池数量上限；**同一资源的任务占用与充电时段不得重叠**；充电按两阶段模型折算且再投入前充至 100%；首批/医疗/期望时限；起飞前 SOC=100%（初始）且任务结束剩余 SOC ≥ 返航电量下限；问题3 追加：全程连续通信、不允许中继多跳、任一时刻仅一路径保障、两段链路同时可用、悬停位置在 DEM 覆盖范围内、悬停离地高度 ≤ 上限、中继满足返航电量；问题4 追加：每服务区属于且仅属于一组、每组非空、同架次多服务区必须同组、保持问题3 安排不变、资源不得跨组调配 |

**关键变量的作用与影响**
- **载荷 `q` 是"三重身份"变量**：它同时进入航程公式（非线性）、能耗公式与载质量约束，因此组批决策会直接改变能耗与返航可行性——这是本赛题不能把"装箱"与"路径"分开做的根本原因。
- **航段沿线 DEM 最高点**是本题最容易被忽略的耦合源：它使航段属性非加性，需要在数据层面实现精确的地形采样；处理不当会系统性低估爬升能耗，导致所有后续结论偏乐观。
- **共享电池 SOC 与充电时间轴**是隐藏的关键路径资源：在 8 架无人机与多架次并行时，电池周转常成为完成时间的实际瓶颈，而非飞行时间本身。
- **通信链路判定参数（门限、遮挡附加损耗、频率）**决定"哪些航段必须依赖中继"，进而决定中继架次数与悬停位置；因此链路参数的小幅变化可能显著改变调度结构（属于结构性敏感参数）。
- **分区方式**通过"资源能否共享"这一机制影响总资源需求：分得越细、越均衡，理论上冗余越小但可能因不可共享而需求上升；这正是问题4 要定量回答的核心机制。

---

## 五、初步建模方向判断

**【可能建模方向】**
1. **基础计算内核（必做且优先）**：
   - 航段几何：节点坐标 → 水平距离 → DEM 沿线采样取最高地面高程 → 巡航海拔（+50 m）→ 爬升/下降高度；
   - 时间：`t = h↑/v↑ + d/vc + h↓/v↓`；
   - 能耗：等效航程 `L_g(q)` 非线性关系 + 水平能耗 + 爬升附加能耗（下降不单独计）；
   - 返航判据与最大安全载荷：对每个 (机型, 服务区) 求 `E_p^T(q)=(1-ρ_g)E_g^use` 的临界载荷（二分/不动点迭代）。
2. **问题1**：三约束装箱化组批——**贪心构造（按体积/质量降序首次适应）+ 局部交换改进**；多目标（架次数、能耗、累计作业时间）用加权和/ε-约束 + Pareto 展示；ρ_g 灵敏度用参数扫描 + 临界值分析。
3. **问题2**：**构造启发式 + 大邻域搜索（ALNS/LNS）** 的非加性能耗 VRP；也可考虑**两阶段分解**（先按地理与时限聚类定"服务区分组"，再对每组做路线与机型/电池分配），并用"电池时间轴可行性检查器"作为解的评价组件；多目标用加权或分层（先满足时限硬约束，再优化完成时间/能耗/架次）。
4. **问题3**：通信可行性的**几何判定 + 链路预算**子系统；把"连续通信"离散化为航段关键采样点约束；中继位置用 **DEM 网格候选枚举 + 局部搜索（或连续优化）**，与运输路线做**交替优化**或统一进入 LNS 的邻域（移动中继位置、变更路线、增删中继架次）；中继能耗含飞行、悬停与通信附加功率。
5. **问题4**：在"架次—服务区"关联结构上做**带约束分区**（服务区数仅 15，可做枚举+剪枝或用带约束的 k-means/图划分）；资源独立核算用"按组重放时间轴"的方式；评价指标自行定义并说明（规模、冗余率、均衡度、缺口），可借用基尼系数/极差/方差。
6. **通用方法层**：由于问题本质是多目标组合优化，建议以 **MILP 建模小规模算例作为下界/校验**（如单机型、少服务区的简化版），主算法采用启发式以保证规模可行性——这既满足"模型合理性"，也满足"可解性"。

**【判断依据】**
- 题目**附录2/附录3 已给出完整的计算口径**（时间、能耗、返航余量、充电折算、链路预算、遮挡判定）→ 建模重心不在"重造公式"，而在**把这些规则实现为可复用的计算内核**，并在此基础上做组合优化；任何自创公式都会与官方口径冲突。
- **能耗非加性 + 载荷递减 + DEM 依赖** → 经典 VRP/TSP 求解器与预计算距离矩阵的做法不适用，必须采用"路径敏感"的构造式启发式与邻域搜索。
- **共享电池是最稀缺的时序资源**（数量有限、充电有时间、占用不可重叠）→ 必须把时间轴可行性作为一等约束，建议"路径解 + 资源时间轴可行性检查"的**双层评价结构**。
- **通信判定是明确的可判定逻辑**（有公式、有门限、有遮挡布尔量）→ 适合实现为确定性判定模块，再在优化中作为约束/惩罚项；中继位置是连续量与几何量，适合枚举+局部精化而非纯梯度法。
- **问题4 明确"保持问题3 安排不变"** → 不需要重新优化调度，只需做**划分 + 独立核算 + 指标比较**，因此应把工作重点放在"关联结构构造、资源独立重放、评价指标定义"上。
- 提交模板已限定六个工作表 → 求解过程必须按表组织输出，建议先固化输出契约再做算法，避免返工。

---

## 六、赛题难点总结

**【核心问题】**
在**能耗约束（非加性、依赖地形与载荷）** 与**通信必须全程连通（时变、依赖三维位置与地形遮挡）** 的双重约束下，如何组织"货箱组批—路线—机型与无人机—电池周转—中继保障"这一整条决策链，使物资既及时送达又总代价可控；并进一步回答"把任务划分为若干独立执行单元后，资源需求与缺口如何变化"。核心矛盾是**及时性、总完成时间、能耗、资源占用四个方向不一致的目标之间的权衡**，而真正的瓶颈往往出在共享电池周转与通信遮挡这两个"隐性能源/通路约束"上。

**【关键难点】**
- **数据**：需要真正解析并使用 30 m DEM（沿线最高点）、箱清单（质量/体积/时限）、链路参数（含遮挡附加损耗）等；参数分散在 5 个 xlsx 与多份地理数据中，口径统一（单位、作业高度、时间基准）是第一道门槛。
- **模型**：能耗非线性且路径依赖，破坏可加性；电池是带时间轴与充放电折算的独立资源；通信约束是连续时空约束（"全程保持"），需要合理离散化并控制计算量；多目标需要明确且可辩护的量化口径。
- **算法**：组合规模（80 箱 / 15 服务区 / 3 机型 / 8 架 / 多架次 / 中继位置连续）无法精确求解；必须构造"可行性判定 + 启发式搜索"的混合框架；通信遮挡判定在大量候选解下可能成为计算瓶颈。
- **应用**：必须输出可直接执行的调度成果（路线、逐箱时刻、资源时间轴、通信保障时段），并按官方模板六个工作表提交；分区问题还需给出资源冗余与缺口的可解释结论。

**【后续建模重点】**
1. **先做计算内核**：把"两节点 → 航段时间/能耗"、"载荷 → 最大安全载荷"、"两个通信端点 → 链路可用性"三个函数做成经过单元测试的模块，全题共用；这三个函数正确性决定所有后续结果的正确性。
2. **建立"路径敏感"的评价函数**：架次评价必须逐航段累计载荷与爬升高度，不允许用简化距离矩阵；同时把返航余量与时限作为硬约束、把能耗与架次数作为目标项。
3. **把电池时间轴可行性作为一等公民**：设计独立检查器（占用/充电不重叠、SOC 边界、初始与结束 SOC），在搜索中作为可行性门或大罚项。
4. **通信约束离散化方案要可辩护**：明确采样密度、关键点选择（爬升段顶点、巡航段中点、投送悬停时段）与保守化处理（对遮挡判定取悲观），并做采样密度敏感性分析。
5. **中继位置与运输路线交替优化**：先固定中继位置优化运输、再固定路线优化中继位置，迭代直至收敛，并给出收敛性与解质量说明。
6. **问题4 提前设计评价指标**：资源规模、冗余率、组间均衡度（方差/极差/基尼）、缺口及其原因，四项指标都要给出公式与解释，避免"只给方案不给评价"。
